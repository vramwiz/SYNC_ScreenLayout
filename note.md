# SYNC_ScreenLayout 開発ノート

- AIによるサムネイル制作へ向け、Named Pipeに`get_canvas_snapshot`、`render_preview`、
  `measure_text`、`list_fonts`、`get_layout_geometry`、`get_creation_schema`を追加した。
  背景、Documentの透明描画、合成結果をローカルPNGで返し、中央原点の文書座標への変換情報を付ける。
  AviUtl2の参照背景はDocument外のため、Canvasから複写する専用APIと`background_token`を追加した。
  画像制作のプレビューと適用ではDocumentと背景の両トークンを照合し、古い背景に基づく案を拒否する。
  未適用プレビューは一時Documentと共通レンダラーを使い、選択とUndoを変更しない。
  文字計測は通常横書きのSkia組版を共有し、表示行、寸法、収まり、解決フォントを返す。
  配置範囲は変形後の幾何範囲で、影・縁取りを含む正確な見た目は合成PNGで確認する。
  作成例は文字（縁取り・影付き）、四角、楕円、三角形Shapeを現行Writerで生成する。
  空き領域・被写体の意味判断は画像を読んだCodexが担当し、アプリに顔検出器は追加しない。
  `CODEX_AUTOMATION.md`を画像取得・配置判断・文字計測・試し描画・一括適用の手順へ更新した。
  従来の「画像を返さない」制限は撤回し、一時PNGの絶対パスを返す。PNGは一時フォルダーに保持する。
  専用テストで画素の上下・色・透明度、座標、プレビュー非変更、背景／文書競合、文字計測、
  作成例JSON、Undo／Redoを確認した。関連3テストと全4構成が警告0・エラー0で成功し、
  Release版Named Pipeの実通信で取得・プレビュー・適用・Undo、PNG表示を確認した。
  AviUtl2配置先auf2とRelease DLLのSHA-256も一致した。

- 開いているScreenDesignMaker／画面レイアウト編集画面をCodexから操作する専用Named Pipe
  `\\.\pipe\ScreenDesignMaker.v1`を追加した。Aul2MIRAIのPipeスレッドライブラリをプロジェクト内へ
  コピーし、ScreenLayout固有の状態取得、完全Document取得、置換プレビュー、一括置換、Undo／Redoを
  JSON命令として実装した。置換は現行Document JSON Readerで一時Documentへ事前検証し、`state_token`で
  同時編集を拒否し、全体を1回のUndo履歴へ登録する。PNGや画面画像は返さず、結果は開いている編集画面へ
  即時反映する。Codexが学習する接続方法、命令、必須手順、サムネイル制作規則を
  `CODEX_AUTOMATION.md`へ記載した。単独Releaseアプリとの実通信で取得、プレビュー、適用、古いトークンの
  拒否、Undo、Redo、復元を確認した。

- 左ツールパレットの最下部へ、次に配置する図形の作成既定色を示す28px角相当の色見本を追加した。
  10個のツールボタン間隔を3px相当へ詰め、既定の58×480px相当のパレット内へ収める。黒と白の二重枠で
  暗色・明色のどちらも判別でき、EditorStateの作成色変更通知で再描画する。96 DPIの実描画ピクセル一致
  テストと全4構成が警告0・エラー0で成功し、AviUtl2へのプラグイン配置も更新した。

- 左ツールパレットの選択中アイコン背景を、ダークポップアップのアクティブ親項目と同じ色へ統一した。
  アクティブ背景色とアクセント色を`VectArtDarkPopupMenu`の公開定数とし、ツール側は数値を重複定義せず
  共通定数を参照する。関連2テストと全4構成が警告0・エラー0で成功し、プラグイン配置も更新した。

- 汎用ダークポップアップライブラリへ、通常、ホバー、表示中サブメニューの親という項目状態を追加した。
  通常項目はマウスがある間だけ背景を強調し、サブメニューを開いた親項目は子側へ移動しても濃い背景、
  左端アクセント、外枠を維持する。別項目へ移るか子を閉じると解除する。背景、文字、無効、ホバー、
  アクティブ親、アクセント色は`VectArtDarkPopupMenu`の共通定数へ集約し、利用側は状態処理を持たない。
  オブジェクト、フィルター、メイン画面の関連3テストと全4構成が警告0・エラー0で成功し、
  AviUtl2へのプラグイン配置も更新した。

- ペン系ツールの右クリックは、削除可能なPath／Shape頂点または幅点に命中した場合だけ頂点削除として
  処理を終了し、頂点に命中しなければ既存のオブジェクトメニュー判定へ渡すようにした。作成中Pathの
  右クリック確定は従来どおり優先する。ペン系ツールのままオブジェクト本体または空白を右クリックしても
  ポップアップを開ける。実キャンバステストで頂点削除時にメニューを出さないこと、非頂点と空白では
  メニューを出すこと、Undo／Redoを確認した。全4構成が警告0・エラー0で成功し、プラグイン配置も更新した。

- オブジェクトの右クリックメニューを「重なり」「反転」「回転」「整列」の短いサブメニュー名へ整理した。
  重なりは最前面、最背面、1段前面、1段背面を既存レイヤー操作へ接続し、反転は既存の左右・上下反転、
  整列は既存の6方向整列と2方向均等配置を使用する。回転は選択全体の共通中心を基準に左／右90度を
  共通レイヤー操作としてUndo可能にし、任意角度指定は保留した。重なり順には一般的なAdobe系と同じ
  `Ctrl+]`／`Ctrl+[`、最前面・最背面にはShift併用を割り当て、既存の反転`Shift+H／V`を維持した。
  ポップアップ専用テストで端移動、90度回転とUndoを確認し、全4構成が警告0・エラー0で成功した。
  起動中アプリを上書きしないようアプリだけ検証用出力先でビルドし、プラグイン配置は更新した。

- カラーピッカーの色履歴とSV領域の間へカラーコード欄を追加した。表示は`#RRGGBB`へ統一し、
  入力は`#RRGGBB`、`RRGGBB`、`rgb(r,g,b)`、`r,g,b`を受け付ける。Enterまたはフォーカス移動で
  確定し、不正値は適用せず赤字で示し、Escで現在色へ戻す。既存の色変更経路を使うため、オブジェクト、
  フィルター、グラデーション点、パターンでもUndo／Redoと色履歴が働く。専用色UIテストへHEX、RGB十進、
  不正値、正規化、Undoの回帰確認を追加した。テストとアプリ／プラグインの全4構成が警告0・エラー0で成功し、
  AviUtl2へのプラグイン配置も更新した。

- オブジェクトの右クリックメニューへ「グループ」サブメニューを追加し、「グループ化」と
  「グループ化解除」を配置した。トップレベル選択と一時的に開いたグループ内選択の可否判定・実行を
  共通関数へまとめ、既存のCtrl+G／Ctrl+Shift+Gと右クリックメニューが同じ処理を利用する。
  メニューからのグループ化／解除とUndoを回帰テストで確認し、アプリWin64 Debug／Releaseビルドが成功。

- 一時的に開いたグループ内で選択した子要素も、グループ化前と同じ属性ツールへ切り替わるようにした。
  通常文字／文字パスは文字設定、開いたPath／線矩形／円弧は線設定を表示する。属性変更処理を積層番号
  ではなく実レイヤー参照で扱えるようにし、グループ内文字の書式とグループ内線の線幅を変更しても
  Undo／Redoできる。表示切替と変更履歴の回帰テスト、アプリWin64 Debug／Releaseビルドが成功。

- 一時的に開いたグループ内の通常文字を、テキストツールで直接クリックして再編集できるようにした。
  編集中の対象はDocument直下の積層番号だけでなく実レイヤー参照でも保持し、描画除外、IME入力、
  キャレット位置、文字反映、確定を同じ経路で扱う。Documentにグループ内文字へ変更通知付きでデータを
  適用するAPIと、レイヤー参照型の文字編集コマンドを追加し、階層を維持したままUndo／Redoできる。
  グループ内文字の実キャンバス編集と関連3テスト、アプリ／プラグイン全4構成のコンパイルが成功。
  配置先auf2使用中のため、プラグインのPost-buildコピーだけ保留。

- 選択枠のリサイズハンドルは、白黒の外周描画が8pxの矩形より外へ広がる一方、ホバーとMouseDownが
  内側の矩形だけを判定していた。共通の`HitTestSelectionHandle`で表示外周3pxを含めるようにし、
  見えている点の端でもカーソル変更と拡大縮小を開始できるようにした。通常文字の右下ハンドル外周を使う
  回帰テストが成功。アプリ両構成とプラグイン両構成のコンパイルは警告0・エラー0で成功した。
  配置先auf2使用中のため、プラグインのPost-buildコピーだけ保留。

- 通常文字をクリックだけで配置した場合は`WrapWidth=0`を「自動幅・折り返しなし」として保持し、
  入力中は明示改行ごとの最長行に合わせて枠を伸ばす。クリック判定はキャンバスとの交差後の矩形ではなく、
  実際のマウス移動量で行う。JSON読込でも0を1へ補正せず、保存・再読込後に1文字幅で自動改行される
  問題を修正した。ドラッグで幅を指定した文字だけは従来どおり指定幅で折り返す。文字作成とJSON往復の
  関連2テスト、アプリ／プラグインのWin64 Debug／Release全4構成が警告0・エラー0で成功し、
  AviUtl2へのプラグイン配置も更新した。

- 色選択パネル全体へフィルター一覧と同じ`TVerticalScrollBarControl`を追加した。96 DPI換算538pxの
  内容高を維持し、割り当て高が不足する場合だけ右端へ表示する。ホイール、バーのドラッグ、トラック、
  キーボード操作で上部の対象設定から下部の色履歴・色相・SVピッカーまで移動できる。十分な高さへ
  戻した場合はスクロール位置を0へ戻してバーを隠す。短いパネル、ホイール移動、最下部到達、復帰を
  色UIテストへ追加した。関連テストとアプリWin64 Debug／Releaseが成功。プラグイン両構成も
  警告0・エラー0でコンパイル成功したが、配置先auf2使用中のためPost-buildコピーは保留。

- 直近追加した色履歴と図形配置プレビューの責務・規模・配置を再点検した。色履歴は表示、文書色収集、
  選択通知を183行の専用ユニット、配置プレビューは一時文書生成と画像キャッシュを130行の専用ユニットに
  留めており、追加分の再分割は不要と判断した。Color直下は共通色UI、種類固有処理はPattern／Texture、
  Creation直下は作成処理4ユニットに分類済みで、追加フォルダも不要。公開APIとキャッシュ状態の所有権・
  副作用コメントを補強した。関連2テストとアプリ全2構成、プラグイン全2構成のコンパイルは成功。
  配置先auf2使用中のため、プラグインのPost-buildコピーだけ引き続き保留。

- 楕円弧の線・塗り図形も実描画の配置プレビューへ追加した。旧円弧輪郭の重ね描きを抑止し、
  色・塗り・透明度・線幅・線種・端形状を反映する。5図形×3塗り方式の確定後RGBA一致テストが成功。
  アプリ両構成とプラグイン両構成のコンパイルは成功。配置先auf2使用中によりコピーは引き続き保留。

- 四角・丸角四角・楕円の配置中に、確定後と同じ塗り・色・透明度のプレビューを表示する。
  ScreenLayoutPlacementPreviewを新設し、独立した一時文書と通常描画キャッシュを使う。
  編集文書・選択・Undoを変更せず、同じ配置範囲とスタイルの画像は再利用する。
  Direct2D／GDIの両方でプレビューへ白黒の点線配置枠を重ね、背景と同色でも範囲を示す。
  3図形×単色・グラデーション・パターンで配置中と確定後のRGBAが一致するテストと関連2テストが成功。
  アプリWin64 Debug／Release成功。プラグインも両構成のコンパイルは警告0・エラー0だが、
  配置先auf2が使用中のためPost-buildコピーは失敗した。AviUtl2終了後に通常ビルドで配置を更新する。

- フィルター一覧へレイヤー一覧と同じTVerticalScrollBarControlを追加した。表示高さを超える場合だけ
  右端へ表示し、ホイール・バー操作・選択行の自動表示に対応する。スクロール分を描画・選択・ON/OFF・
  スライダー・並べ替えの座標へ反映し、削除・リサイズ・レイヤー交換時に移動量を補正する。
  狭いパネルでは色ピッカーの高さを最小値まで縮め、補助設定より先にフィルター一覧1行分を確保する。
  専用スクロールテストとメニュー・画面生成の関連2テストが成功。全4構成が警告0・エラー0で成功し、
  AviUtl2へのプラグイン配置も更新した。

- カラーピッカー直上に基本16色と色履歴を追加した。直近16色を表示し、古い履歴はホイールで移動する。
  エディタ表示直前とJSON読込後に文書の使用色を取り込み、グループ内・グラデーション中間点・
  パターン・フィルター・図形縁取り・不透明なキャンバス背景を収集する。履歴は編集セッション内で保持する。
  ピッカーのドラッグ終了または色見本クリックで確定色だけを先頭へ追加し、同色は重複させない。
  履歴の描画と収集はScreenLayoutColorHistoryへ分離し、既存の色適用・Undoの経路を使用する。
  ピッカー領域との重なりを避ける最小高さを設定した。関連3テストと表示画像を確認済み。
  色テストにあった未初期化のPaintStyleを修正した。全4構成が警告0・エラー0、プラグイン配置も成功。

- Path／Shape／TextPathの作成中、始点から画面上8px以内では始点への点スナップを優先する。
  角度・グリッド・他オブジェクトによる軸スナップとの競合を避け、十字ガイドで吸着位置を示す。
  Altのスナップ解除とShiftの方向制限を維持する。Shapeは既存の始点クリックによる確定へ接続する。
  スナップテストで3ツール・2倍率・クリック位置・修飾キー・図形確定を検証し、作成重なりテストも成功。
  アプリ／プラグイン全4構成が警告0・エラー0で成功し、AviUtl2のプラグイン配置も更新した。

- 選択済み頂点への右クリックはペン以外でも頂点削除を優先する。Path／Shapeそれぞれで命中した頂点と
  選択インデックスを照合し、別の未選択頂点ではオブジェクトメニューを維持する。削除は既存のUndo履歴へ登録する。
  実キャンバステストで両種の削除、Undo／Redo、未選択頂点のメニューを検証した。
  アプリ／プラグインのWin64 Debug／Release全4構成が警告0・エラー0で成功し、プラグイン配置も更新済み。

このファイルは、作業再開時に必要な未完了事項、設計方針、プロジェクト構成、ビルド規約、コメント規約を記録する。

- ペン系ツールの右クリックは頂点削除へ優先的に渡し、未命中でもオブジェクトメニューを出さない。
  オブジェクトメニューは選択ツールで表示する。Pathツールでも閉じたShapeの頂点追加・削除を有効にした。
  作成中のPathを右クリックで確定する操作は維持する。実キャンバスの専用テストと頂点編集・作成・幅点の
  関連3テストが成功。アプリ／プラグインのWin64 Debug／Release全4構成が警告0・エラー0で成功した。
  AviUtl2への配置も更新でき、前回保留だった選択色とユニット整理を含む最新版を配置済み。
- レイヤー編集周辺の責務とフォルダを整理した。`Source/Layers/List`へ一覧、行配置、行描画、
  サムネイルキャッシュ、配色・寸法、クリック選択を分類し、`Operations`へ操作バーと操作調整、
  `Clipboard`へ専用クリップボード転送を分類した。`Source/Layers`直下はパネルFrameだけとする。
  `ScreenLayoutLayerRenderer`は973行から538行、`ScreenLayoutLayerList`は882行から765行へ縮小した。
  行描画417行、サムネイル148行、選択172行へ責務を分け、描画APIと入力状態を相互依存させない。
- 貼り付けの所有権と履歴は`ScreenLayoutInsertObjectsCommand`、文書直下の一括削除は
  `ScreenLayoutDeleteLayersCommand`へ分離した。転送処理は178行で、WindowsクリップボードとJSON検証を担当する。
  `Source/Core/Commands/Layer`は共通履歴4ユニット、`Group`は階層操作3ユニット、`Transform`は
  変形・反転・整列3ユニットに分類した。アプリ／プラグインのdpr・dprojとテストの参照先を更新した。
- 分離したクラスの公開APIへ責務・副作用、フィールドへ所有権・状態の意味、共通定数へ用途を記載した。
  テスト3本に残っていたEditHistoryの古い参照先も現行Modelフォルダへ修正した。関連15テストが成功。
  アプリWin64 Debug／Releaseは警告0・エラー0。プラグインも両構成のコンパイルは警告0・エラー0だが、
  使用中の配置先auf2へ上書きできずPost-buildは失敗した。AviUtl2終了後の通常ビルドで配置を更新する。
- レイヤー一覧の選択背景を青系へ強め、アクティブ行はさらに明るい青の背景、水色の外枠、4px相当の
  左端マーカーで区別する。GDI／Direct2Dの両描画へ適用した。アプリのDebug／Releaseビルド成功。
  プラグインも両構成のコンパイルは成功したが、配置先auf2が使用中のためPost-build配置は失敗した。
  次回、AviUtl2終了後に通常ビルドで配置を更新する。
- レイヤー一覧の通常クリックは、選択済みの複数行からドラッグを始めなかった場合、MouseUpで
  クリック行だけの単一選択へ戻す。ドラッグでは一括選択を維持し、ロックを含む選択でもクリックによる
  単一選択へ切り替えられる。Ctrlは追加／解除、Shiftは範囲、Ctrl+Shiftは範囲追加とする。
  グループの開閉は修飾キーなしのダブルクリックだけで行い、Ctrl／Shift操作を奪わない。
  範囲の基準点は親階層ごとに扱い、グループ内の初回Shiftは現在の子選択を基準にする。
  子選択時には文書直下の古い選択を解除し、空白の通常クリックは現在選択を解除する。
  レイヤー操作・階層移動・行位置・グループ・クリップボード・ショートカットの6テストと全4構成を検証した。
- オブジェクトのコピー／貼り付けを専用Windowsクリップボード形式
  `ScreenDesignMaker.Objects.JSON.v15`へ接続した。画像や通常テキスト形式は交換せず、選択レイヤーの
  内部JSONを独立したスナップショットとして保持する。複数選択は積層順、グループは子孫ごとコピーする。
  貼り付けは現在の階層の最前面へ元の文書座標で配置し、挿入物を選択する。1回を1件のUndo／Redoにする。
  Ctrl+C／V／X、Deleteと右クリックのコピー・貼り付け・切り取り・削除を接続し、既存の複製も接続した。
  空白上の右クリックでも貼り付け可能。Ctrl+Aは開いているグループの直下だけを選択する。
  グループを含む混在選択の削除と選択順に依存しない一括削除を修正し、ロックを含む選択の変更は無効にした。
  文字編集中は文字側のキー処理を優先する。クリップボードが一時占有される場合は短時間だけ再試行する。
  専用クリップボード・ショートカットテストと関連5テスト、アプリ／プラグインの全4構成を検証した。
- 開いたPathの単一選択時は、右クリックの「線を閉じたパス図形へ変換」で線の表示外周をShapeへ
  置換できる。均一幅と破線はSkiaのストローク輪郭、可変幅の実線と三角端は既存の可変幅外周を使う。
  線色を塗りへ移し、共通ペイント、不透明度、フィルター、変形を保持する。トップレベルと開いた
  グループ内の置換、Undo／Redoに対応する。丸端は半円を2本の3次ベジェで生成し、左右幅が異なる
  可変幅でも各象限の半径を保つ。均一幅の丸端Conicも輪郭変換時だけ1本の3次ベジェへ近似する。
  可変幅の外周点列は0.25文書座標の許容差で簡略化し、直線区間の密集頂点を除く。
  鋭角では元のPath頂点の区間長位置を描画サンプル列へ必ず挿入し、頂点前後の左右オフセット線を
  交点で結合する。交点より角側にあるサンプルは交点へ畳み、簡略化で除去する。外側のマイター長は
  線幅比4倍までとする。Debugビルドで線を閉じたパスへ変換すると、元頂点、幅点、最終輪郭を
  `%TEMP%\ScreenDesignMaker-stroke-outline-debug.log`へ上書き出力する。
  実ログでは均一実線のSkia展開結果に元の中心頂点が2個混入して内側の穴を作っていたため、均一実線も
  自前の可変幅外周生成へ両端100%として渡す。破線だけはSkiaのストローク展開を使用する。
  `LineCap=Round`では途中の外角へ角位置の幅を半径とする円をUnionし、ラウンド結合にする。Shape変換時は
  0.1文書座標以内の演算境界点を統合し、円弧Conicを3次ベジェへ変換する。均一幅の開いた実線も
  通常描画から両端100%の同じ外周生成へ渡し、分解前後の結合形状を一致させる。
  自己交差のブーリアン正規化は次段階とする。専用テストと
  可変幅関連3テスト、アプリ／プラグインの全4構成が成功し、配置先プラグインのハッシュも一致した。
- 可変幅Pathの単一選択時は、中心線上の幅点、進行方向左右の幅ハンドル、両者を結ぶガイドを表示する。
  左右ハンドルのドラッグは片側の0～100%倍率だけを変更する。Ctrl+クリックは中心線の最近傍へ補間済み
  幅点を挿入し、中間の中心ハンドルの右クリックはその幅点を削除する。両端点は削除しない。各操作は
  `TScreenLayoutPathWidthPointsCommand`でUndo／Redoする。中心線の鋭角／ベジェ編集とは独立して扱う。
  幅ハンドル、可変幅、ツールバー、自由変形など関連10テストと全4構成のビルドが成功した。
- 線属性ツールバーへ均一幅と可変幅を表す2個のアイコンを常設し、既定を均一幅とした。線、自由線、
  Pathの作成中はEditorStateの初期値を切り替え、選択ツールでは開いたPathの幅プロファイル有無を
  Undo可能な操作として切り替える。筆圧なしの可変幅作成は両端100%の幅点を作るため、見た目は
  均一のまま次段階の幅点編集へ移行できる。円弧や矩形線など未対応種別ではアイコンを無効化する。
  幅モード、可変幅描画、ツールバーDPIなど関連8テストとアプリ・プラグインの全4構成が成功した。
- 開いたPathは任意の全長位置に進行方向左右の幅倍率を持てる。自由線ではWindowsペン入力の筆圧を
  0～100%の対称幅として記録し、作成中と確定後の描画へ反映する。筆圧のないマウス入力では幅情報を
  作らず、設定線幅の均一線とする。幅プロファイルはJSON、Undo／Redo、複製、反転で保持する。
  現在は開いた実線Pathだけを可変幅描画し、破線は均一幅へフォールバックする。幅点を直接編集する
  キャンバスUIと筆圧カーブ設定は次段階とする。
  可変幅入力・モデル・JSON・複製・描画と関連機能の計5テスト、アプリ・プラグインのWin64
  Debug／Release計4構成が成功し、ReleaseプラグインとAviUtl2配置先のSHA-256も一致した。
- 自由線ツールでは、設定線幅をズーム換算した白黒の円と中心十字をマウスへ追従表示する。ペン接触中は
  円の直径も現在筆圧へ追従する。自由線の作成中は現在の線色と筆圧幅、通常Pathは設定線幅で表示する。
- 自由変形済みレイヤーでは、射影座標へ未対応の角丸半径、円弧角度、字間・行間ハンドルを表示しない。
  Path／Shapeの頂点は変形座標へ対応済みのため引き続き表示する。
- 通常文字をクリックだけで作る場合は、確定文字列の最長行に合わせて入力枠と`WrapWidth`を横へ伸ばす。
  ドラッグで幅を指定した場合だけ、その幅で自動折り返しする。
- 自由変形追加後、全43テストとアプリ・プラグインのWin64 Debug／Releaseのコンパイルに成功。
  完成整理後にも同じ検証を再実行し、全43テスト、全4構成とも成功した。
  Release版アプリと配置先auf2は通常出力先へ更新済み。
- 単一オブジェクト移動は、下層・対象・上層をドラッグ開始時にRGBA8へ保持し、移動中は対象画像の
  オフセット合成だけを行う。閉じたPathおよび背景に残る重いPathを毎フレーム描画しない。
  通常座標移動と自由変形済みオブジェクトの移動に対応し、確定時だけベクターで再描画する。
  複数選択と開いたグループの子移動は従来描画を使用するため、必要なら同じ方式を拡張する。
- ホイールの連続ズーム中は直前の文書画像を拡縮して表示し、最後の入力から140ms後に現在倍率で
  ベクターを再描画する。選択枠などの編集オーバーレイは待たずに新しい倍率へ追従する。
- 通常文字のIME変換中はCanvas側の編集キー処理を止め、変換確定のEnterや候補操作をIMEへ渡す。
  `ScreenLayoutCanvasCreationOverlapTest`で、変換確定時にDocumentへ改行が入らないことを確認する。

- 利用者向けの機能説明: [README.md](README.md)
- 実装状況の時点記録: [HISTORY.md](HISTORY.md)
- 参照元: `D:\DelphiProg\test\VectArtDesigner\note.md`
- パターン機能の打ち合わせ資料:
  `D:\DelphiProg\SYNC_ScreenLayout\ScreenDesignMaker_Pattern_Discussion_20260906.md`

追加機能の過去の検討内容、未実装項目、将来の設計方針は、本ノートの
「今後追加予定の機能」へ統合済みである。

- `Setup/make_release_zip.bat`は、ReleaseビルドでAviUtl2へ配置したプラグイン本体と`sk4d.dll`を
  `SYNC_ScreenLayout`ルートフォルダへまとめ、`Setup/SYNC_ScreenLayout.zip`を生成する。
  `CODEX_AUTOMATION.md`も同じフォルダへ同梱し、別環境のCodexへ操作仕様と実制作の知識を配布する。
  ガイドはプロジェクト直下の最新版を取得し、存在しなければZIP作成を失敗させる。
  `Setup/SYNC_ScreenLayout.catalog.json`はGitHub Releaseから同名ZIPを取得し、
  `{pluginsDir}/SYNC_ScreenLayout`へ配置する。初回カタログ版は`v1.0.0`、2026-09-06とし、
  主プラグインのXXH3-128で検出する。ライセンス宣言、カタログ画像、公開済みGitタグがないため、
  ライセンスは「不明」、画像は未指定としている。正式公開時は公開したZIPからハッシュを再確認する。
- フィルターの色編集対象は、そのフィルターを持つレイヤーが現在の単一選択である間だけ有効とする。
  別のテキストや図形を選択した場合は`ValidateSelectedFilter`で以前のフィルター選択を解除する。
  同じ所有レイヤーでもキャンバス上の本体またはレイヤー一覧の行を選び直した場合はフィルター内選択を解除し、
  文字・図形本体の色へ切り替える。フィルターの直接編集ハンドルを操作した場合はフィルター選択を維持する。
  青いテキストと赤い縁取りの再現テストを含む関連4テスト、およびアプリ・プラグインの全4構成ビルドに成功。
- フィルター追加の＋はVCL標準`TPopupMenu`を廃止し、他のメニューと同じ
  `TVectArtDarkPopupMenu`で縁取り・影・ぼかしを表示する。外側クリック、Esc、非アクティブ化は
  `TVectArtDarkMenuGroup`で閉じ、＋の再クリックでも閉じる。項目選択後の追加とUndo／Redoを専用テストで確認した。
- グラデーション種類とテクスチャ設定の文字は、Segoe UIの12px相当を基準に`CurrentPPI`で拡大する。
  グラデーションの行高、文字余白、矢印と、テクスチャの画像余白、ファイル名欄、設定行、エラー表示も
  同じ倍率へ統一した。HWND生成前に寸法を確定し、コンボボックスの再生成を発生させない。
  プラグインDPI分離、色UI、テクスチャ、メイン画面生成の4テストと全4構成のコンパイルに成功。
- 単独アプリはPer-Monitor V2 DPI対応とし、標準マニフェストをプロジェクトリソースとしてリンクする。
  Windowsのビットマップ拡大を使用せず、モニターの実DPIで文字とVCLコントロールを描画する。
  独自描画UIの座標、フォント、クリック判定も`CurrentPPI`へ揃える。レイアウト設定の寸法は
  96 DPI換算で保存し、読み込み時に現在のモニターDPIへ変換する。version 1の既存設定も同様に移行する。
  200% DPIで単独アプリの左右ペイン、上部操作列、ツールパレット、色UI、編集メニューを確認済み。
  線・文字ツールバーは選択状態の更新時にも論理幅をDPI変換し、太さスライダーを切り取らない。
  レイヤー一覧は行高、余白、サムネイル、状態アイコンをDPI変換し、クリック判定、スクロール、
  ドラッグ判定も描画と同じ倍率を使う。
  プラグイン編集FormはAviUtl2のDPIコンテキストから分離するため、生成から破棄までスレッドを
  `DPI_AWARENESS_CONTEXT_UNAWARE_GDISCALED`へ一時切り替え、終了時にホストの元のコンテキストを復元する。
  API非対応環境は従来動作へフォールバックする。
  DPI分離、メイン画面生成、ドック幅、レイヤー一覧の4テストと、アプリ・プラグイン全4構成のビルドに成功。
- 色UIは上部固定、中央可変、下部固定の3領域とする。上部は対象見出し、「透明度：」とスライダー、
  選択色、ベタ・グラデーション・パターン・テクスチャの順。数値と「不透明度」は表示しない。
  中央はモード固有設定だけを切り替え、下部の色相・SVピッカーは常に同じ位置へ表示する。
  テクスチャでは固定表示を維持したまま色操作を無効化する。色Frameは538px相当の固定高とし、
  モード切り替えでフィルター領域を押し上げない。
  固定位置テストを追加し、共通色、パターン、テクスチャ、メイン画面生成テストと全4構成のビルドに成功。
- フィルター一覧下の追加ボタンは32×26px相当に拡大し、大きな＋を描画する。削除ボタンは文字のXを廃止し、
  DPIと有効状態に追従するゴミ箱の線画へ変更した。追加・削除処理とヒントは従来どおり。
  ボタン構成テスト、フィルター色UIテスト、メイン画面生成、アプリとプラグインの全4構成ビルドに成功。

## 次回の再開ポイント

- 自由変形・せん断・遠近変形は2026-09-06に実装した。下記の操作と保存方針を使用する。
- 複数選択の整列と均等配置は2026-09-06に実装した。実機では回転した大小オブジェクトを混在させ、
  外接範囲を基準にした整列と負の均等間隔になる重なり配置を確認する。
- 独自ダークポップアップは名称、ショートカット、サブメニュー記号を別の配置領域で描画する。
  実機では高DPI時の列幅と長い名称の省略表示を確認する。
- 左右・上下反転は2026-09-06に実装した。上部ツールバー、右クリックの「変形」、Shift+H／Shift+Vから
  同じ操作を呼ぶ。実機では反転した通常文字の範囲選択表示とIME入力位置を重点的に確認する。
- パターン機能は2026-09-06に完成とした。下記の実装範囲、確定した仮仕様、残る課題を確認して再開する。
  次回は高DPI・低い画面・AviUtl2実機での操作確認と、必要に応じたキャッシュ・UIの調整を検討する。
- テクスチャ機能は2026-09-06に完成とした。確定仕様と既知の制約は下記を参照する。
- グラデーション拡張は2026-09-06に完成とした。確定した仕様と既知の制約は、
  下記「グラデーション拡張」を参照する。
- 実機で、内部グループへ入った直後のUndo／Redoを確認する。
- Escによる段階的な親グループ復帰を確認する。
- 既存のトップレベルグループを含む再グループ化を確認する。
- 上記操作時に、ショートカット入力、レイヤー一覧、選択枠が同じ状態へ追従することを確認する。

## 自由変形・せん断・遠近変形（2026-09-06実装）

- 選択ツールで、Ctrl+四隅は角の単独移動、Ctrl+辺中央はせん断、Ctrl+Alt+四隅は対称遠近変形とする。
  Ctrl系のShift併用は水平・垂直・45度制約。CtrlなしのShiftは縦横比維持、Altは中心固定とする。
  Ctrlによる変形モードは押下時に決め、ドラッグ中のShift／Altで制約を切り替える。
- `ScreenLayoutProjectiveTransform`が四辺形間の射影変換と逆変換を担当する。
  元の文字・図形データを保持し、各レイヤーの`Transform`へ表示変形を保存する。
  グループと複数選択は同じ変換を子孫へ適用し、グループ自体へ二重に変換を適用しない。
- `ScreenLayoutTransformInteraction`が選択枠、修飾キー、開始時からのプレビュー計算を担当する。
  四隅が交差・退化する操作は最後の有効な状態で止める。Escはフォームのショートカットより先に取消する。
  `ScreenLayoutLayerTransformCommands`は文書内の階層位置と変形前後を保持し、削除Undoでレイヤーが
  再生成された場合も同じ対象を解決する。
- version 15 JSONの省略可能な`transform`配列（9要素）へ保存する。未設定は恒等変換とする。
  復元用の各Dataレコードにも変形を保持する。文字分解・図形化は生成物へ変形を引き継ぎ、
  ブーリアン演算は変形後のPathを使用する。元の型と変形はUndoで復元する。
- 変形後の枠、クリック判定、Path／Shapeの頂点表示とドラッグ、文字のクリックとIME位置を対応させた。
  変形文字の文字選択範囲は、元の矩形文字を重ねず変形後の青い輪郭で示す。

## 整列と均等配置（2026-09-06実装）

- 右クリックの「整列と均等配置」は同階層で2個以上を選択した場合だけ表示する。ショートカット、
  上部ツールバー、専用アイコンは設けない。ロックを含む選択では項目を無効にする。
- 2個以上の選択を、全体の外接範囲を基準に左端、水平中央、右端、上端、垂直中央、下端へ揃える。
  回転したレイヤーは画面上の外接範囲を使用し、閉じたグループは1個の対象として移動する。
- 水平・垂直の均等配置は3個以上で有効にする。対象を軸方向の中心位置で並べ、両端を固定したまま
  中間対象を移動し、形状の大きさが異なる場合も輪郭間の空白を同じ値へ揃える。利用可能範囲が不足する
  場合は負の間隔として均等に重ねる。
- 開いたグループでは選択した直下の子だけを対象にし、各操作の移動全体を1件のUndo／Redoへまとめる。
  専用テストで6方向整列、選択順に依存しない均等配置、両端固定、履歴、グループ内選択、ロックを確認する。

## 左右・上下反転（2026-09-06実装）

- 単一選択は自身の外接範囲中央、複数選択は全選択の外接範囲中央を軸にする。閉じたグループは子を
  再帰的に反転し、開いたグループでは選択した直下の子だけを共通軸で反転する。ロックを含む選択は無効。
- 図形と画像は座標を反映し、回転角、角半径、円弧端、Path制御ベクトル、影のオフセットも反転する。
  通常文字は字形反転状態を保持し、文字パスは経路と字形方向を反映する。線形グラデーション座標、
  パターン、テクスチャも同じ見た目になるよう反転状態を保持する。
- 1回の操作をスナップショット方式の1件の履歴とし、Undoは演算をやり直さず変更前の値を正確に戻す。
  共通レイヤー、パターン、テクスチャの反転状態はversion 15 JSONの省略可能な真偽値として保存する。
- 通常文字の描画、クリック位置、キャレット、選択範囲、アウトライン化、文字分解へ反転座標を接続した。
  専用テストで複数選択軸、各形状、塗り、影、グループ内選択、JSON、Undo／Redo、文字描画を確認した。
  専用テストを含む全41テストと、アプリ／プラグインのWin64 Debug／Release計4構成が通過した。

## パターン機能（2026-09-06完成）

### 実装範囲と操作

- 内蔵6種（斜線、ドット、格子、市松、波線、ハニカム）を共通の塗り方式へ追加した。
  図形・文字・線に適用できる。外部画像は引き続きテクスチャで扱い、パターンには埋め込まない。
- 色パネルの左から3番目のパターンアイコンを有効化した。選択中だけ専用UIを表示する。
  上から3列2行の種類ボタン、透明部分が分かるプレビュー、色スロット、種類別の数値を表示する。
- 「線の色」「点の色」「前景色」と「背景色」を選択して、既存の色相・SVピッカーで編集する。
  上部の透明度スライダーは選択した色スロットを対象とする。対象名と数値は同じ行へ表示しない。
  背景の初期不透明度は0%。
  レイヤー自体の不透明度は別の値として維持し、描画時に乗算する。
- パラメーターはアプリ共通の`Lib/HorizontalTrackBar/THorizontalTrackBarControl`で編集する。
  数値入力欄と数値行の独自ドラッグを廃止し、項目名とスライダーを横並びにした。数値表示は設けない。
  ドラッグで範囲内の値を選び、矢印キーで0.1、PageUp／PageDownで1ずつ調整する。Home／Endは上下限へ移動する。
  項目が収まらない場合は、アプリ共通のLib/VerticalScrollBar/TVerticalScrollBarControlで
  パラメーター領域を縦スクロールする。標準スクロールバーは使用しない。
  スライダー上のホイールによる値変更は無効にし、スクロールと混同しないようにした。
- スライダー変更後はパターンUI、共通色UI、テクスチャ、メイン画面生成の4テストを再実行して成功。
  横並び・数値非表示・汎用スクロールバーへの変更後も同じ4テストが成功した。
  アプリとプラグインのDebug／Releaseビルドは警告・エラー0件。起動中のアプリを維持するため、
  今回のアプリDebug版はWin64/PatternTests/AppDebugへ出力した。Releaseプラグインの配置とハッシュ一致も確認済み。
- 線幅、間隔、移動量などの広い範囲は、既定値や0付近を細かく操作できる曲線へ割り当てた。
  スライダー上のホイールと矢印キーは0.1刻み、PageUp／PageDownは1刻みで論理値を直接変更する。
  急な振り切れ、最大端、ホイール正逆をパターンUIテストへ追加し、関連4テストと全4構成のビルドに成功。
  アプリ・プラグインのDebug／Releaseは通常出力先で警告0・エラー0、プラグイン配置先とのハッシュも一致した。
- パラメーター・色・色の不透明度の連続変更は1件のUndo／Redoへまとめる。マウス捕捉が外れた数値操作も確定する。
  複数選択はまとめて適用し、ロックを含む選択のUIを無効にする。
- 選択対象へ適用した設定は次回作成既定にも採用する。作成ツール使用中は作成既定だけを変更する。
  単色・グラデーション・テクスチャへ切り替えた後も、編集中のパターン設定を保持する。
- パターン種類そのものを変える場合は、その種類の数値を既定値へ戻し、共通IDの色と不透明度を引き継ぐ。
  各種類別に過去の数値を記憶する機能は初期実装に含めない。

### 数値の単位・上限と仮仕様

- 寸法と移動量は文書の論理単位、角度は度で保持する。ズームや出力解像度を変更しても保存値は変えない。
  オブジェクト中心を模様の配置原点とし、模様の角度、オフセット、オブジェクトの回転を順に反映する。
  オブジェクトの拡大縮小では輪郭が変わり、模様の論理的な大きさは維持する。

| 種類 | 固有設定（初期値） |
| --- | --- |
| 斜線 | 線幅2、間隔16、角度45度 |
| ドット | 点サイズ6、間隔16 |
| 格子 | 線幅2、横間隔24、縦間隔24 |
| 市松 | マスサイズ16 |
| 波線 | 線幅2、振幅6、周期32、行間隔24 |
| ハニカム | 六角形半径14、線幅2、間隔0 |

- 全種類で角度と横・縦移動を公開する。斜線以外の初期角度と移動量は0。
  線幅・点サイズは0.1～1024、一般の間隔・マスサイズ・周期は1～1024、
  振幅は0.1～256、六角形半径は1～256、ハニカムの間隔は0～256とする。
  角度は-180～180、移動は-4096～4096、各色の不透明度は0～1。
- 間隔は中心同士の距離として扱う。ドット径は描画時に間隔まで、線幅はタイルの短辺までに制限する。
  ハニカムの線幅はさらに六角形半径までに制限する。保存値は維持するため、間隔を広げれば本来の幅へ戻る。
- ハニカムの「間隔」は半径に対して中心配置のピッチを広げる仮仕様である。
  隣り合う辺の最短距離そのものではない。0なら隙間のない蜂の巣配置になる。
- 波線は正弦曲線を周期的に並べる。独立した行間隔を追加し、振幅が行間隔を超える設定も許容する。
  太い線でも端点が周期境界に露出しないよう、タイルの外まで曲線を生成する。

### モデル・保存・共通描画

- `TScreenLayoutPaintStyle.Pattern`に、安定したパターンID、ID付き数値配列、ID付き色配列を保持する。
  配列の取得・更新はコピーし、複製や履歴スナップショットを後から変更しない。
- `ScreenLayoutPatternStyle`が数値の定義、色スロットの既定値と表示名を公開する。
  初期6種は2色だが、保存・UIは定義の配列を列挙するため3色以上の定義を追加できる。
- version 15 JSONは`paint.type = "pattern"`、`patternId`、`parameters`、`colors`を保存する。
  展開画像とキャッシュは保存しない。未知ID、重複、必須項目の欠落、型違い、非有限値、範囲外を
  Document置換前に拒否する。古い実行ファイルで新パターンを読み込めないことは許容する。
- 現在選択中の塗り方式だけを保存する既存方針を維持する。別の塗り方式へ切り替えて保存・再読込した場合、
  非選択だったパターンの保持値は復元しない。
- 1周期のベクター形状をSkiaのラスタータイルへ展開し、繰り返しシェーダーとして輪郭内へ描画する。
  通常描画、文字、線、単体・グループのサムネイル、AviUtl2出力は同じ描画経路を使う。
- 解像度は必要倍率を1/4オクターブ単位で切り上げる。タイルは一辺8～1024pxに制限し、線形補間する。
  配置行列は論理周期を維持するため、この解像度制限で模様の寸法自体は変わらない。
- キャッシュは描画呼び出しのスコープごとに作成し、同じID・全数値・全色を共有する。
  必要解像度はスコープの倍率として分離する。最大8画像、保有ラスターは最大32 MiBで古い項目から破棄する。
  生成中の画像やPaint側が参照する画像は、このキャッシュ保有量の外となる。
- キャッシュをスレッド間で共有せず、入れ子描画では元のスコープへ戻す。
  描画終了時にスコープを破棄するため、Documentを閉じた後やSkia解放後へキャッシュ画像を残さない。
- モデル、描画、JSON、専用UI、UI描画、共通塗りの連続履歴を6ユニットへ分離した。
  Model／Rendering/Paint／ObjectProperties/Colorのパターン専用コードはそれぞれPatternフォルダへ分類した。
  追加コードはユニット先頭、公開API、フィールド・定数へ責務と制約のコメントを記載した。
- 完成時の肥大化点検では、入力とプレビュー生成を同じユニットに持っていた設定コントロールを分割した。
  パターン固有ユニットは93～269行となった。各Patternフォルダは1～3ユニット、既存フォルダも最大9ユニットで、
  用途別分類を保っている。共通カラーピッカーと色Controllerは状態同期を一か所に保つため、追加分割しない。

### 残る課題・実機確認

- 当初案のDocumentを開いている間のフレーム間キャッシュは未実装。
  初期実装は1回の描画内だけで共有し、安全な破棄を優先した仮仕様とする。
  動画の連続出力、多数の異なる模様、極端な振幅と狭い行間隔を組み合わせた性能評価を継続する。
- 一辺1024pxの制限により、大きな周期を高倍率で表示した場合は細部がぼけることがある。
  縮小時の密な模様のモアレ対策、キャッシュ容量と解像度上限の調整は今後の課題。
- 初期UIは種類名ボタン＋選択中のプレビューとした。種類ごとのサムネイル一覧やコンボは追加していない。
  最大7項目の波線ではパネルが96 DPI換算538pxになる。設定領域の縦スクロールは実装済み。
  低い画面・高DPIでのパネル全体の配置は引き続き実機確認する。
- キャンバス上の模様の直接操作、プリセット、利用者定義の模様は未実装。属性パネルから全数値を操作する。
- 縁取り・影などフィルター自身の色は従来の単色モデルのまま。今回のパターンを含む共通PaintStyleへ
  接続するためのフィルターモデル・UI拡張は別途必要。パターンを持つ対象への既存フィルター適用は維持している。
- AviUtl2映像コールバックの一致は内部テストで確認済み。AviUtl2本体上でのマウス操作、
  高DPI表示、長時間編集・出力については実機確認が残る。

### 検証

- `ScreenLayoutPatternTest`: 6種類の描画、背景透過、色とレイヤーの不透明度乗算、全数値の上下限、
  倍率による解像度変更、保存・復元の画素一致、不正入力時の文書保持、配列コピー、複製、グループとサムネイル。
- `ScreenLayoutPatternUiTest`: 種類別UI、色スロット、複数選択、数値・色・不透明度の連続Undo／Redo、
  汎用スライダーと0.1刻みのキー操作、スクロール後の値変更、数値入力欄の撤去、
  塗り方式を戻したときの設定保持、ロック、作成既定、再入する表示更新。
- `ScreenLayoutPluginRenderParityTest`へパターン6種を含む4番目の出力パスを追加し、AviUtl2コールバックの画素一致を確認した。
- 既存のテクスチャ、グラデーション、フィルター、作成、グループ、スナップ、頂点編集、文字操作を含む計17本が通過。
- アプリ／プラグインのWin64 Debug／Release計4構成が警告0・エラー0。
  Post-build配置されたauf2とsk4d.dllはビルド元とハッシュ一致。
- 完成時の再検証では利用者が起動中の`ScreenDesignMaker.exe`を終了せず、アプリDebugだけを
  `Win64/PatternTests/AppDebug`へ出力した。同じDebug構成は成功し、Releaseとプラグイン2構成は通常の出力先で成功した。
- 検証ログと目視確認用の`patterns.png`／`pattern-ui.png`は`Win64/PatternTests`へ出力する。

## テクスチャ機能（2026-09-06完成）

### 実装範囲と操作

- 共通色UIの右端「テクスチャ」を有効化した。このモードだけ画像専用のドロップ領域と設定を表示する。
  領域へ画像1ファイルをD&Dするか、領域をクリックしてファイル選択する。PNG／JPEG／WebP／BMPを
  ファイル選択に表示し、実際の受け入れ可否は同梱Skiaデコーダーで検証する。
- D&D領域は画像プレビュー部分に限定し、受け入れ中は枠色を変える。複数ファイルは受け付けない。
  通常キャンバスへのD&Dは、従来どおり画像レイヤー追加とする。
- 選択中の塗り対応オブジェクトへ画像を適用する。複数選択は1件のUndoとしてまとめる。
  選択なし・作成ツールでは次回作成用の既定値になる。ロック対象の画像設定は無効にする。
- 「配置」は全面（cover）→全体（contain）→引き伸ばし→原寸の順で切り替える。
  「繰り返し」はなし（画像外を透明）→通常→鏡像の順で切り替える。いずれもダークテーマの行をクリックする。
- 拡大率の左右で1.1倍／1÷1.1倍、回転の左右で15度ずつ変更する。配置リセットは位置・倍率・回転を戻す。
  解除は埋め込み画像を外し、透明な未設定テクスチャへ戻す。読込失敗時は直前の画像・配置を保持し、
  エラーを表示する。詳細は画像設定欄のヒントで確認する。
- 単一選択時、キャンバス上の丸ハンドルで画像中心を移動し、四角ハンドルで拡縮・回転する。
  Shift併用で15度刻みに揃える。オブジェクトの回転と入れ子グループの子選択を参照する。
  ガイドは白黒の二重表示で、ハンドルのホバーは移動カーソルにする。連続ドラッグは1件のUndoにまとめる。
- 不透明度は従来のレイヤー不透明度を使用し、画像本来のアルファへ乗算する。
  図形・文字・線の共通描画、グループ、サムネイル、AviUtl2出力に同じ画像配置を適用する。
- version 15 JSONのpaint.typeにtextureを追加した。画像のBase64、表示用ファイル名、配置・繰り返し・倍率・
  中心オフセット・回転を保存する。外部ファイル参照は持たず、元ファイルを変更・削除しても影響しない。
  数値の型・有限性・範囲、列挙値、画像データはDocument置換前に検証する。旧実行ファイルはtextureを読めない。
- 画像文字列はDelphiの不変共有文字列で保持するため、複製やUndoスナップショットの変更が元へ波及しない。
  表示用プレビューは最大256×128へ縮小し、UIはSkiaオブジェクトを保持し続けない。

### 仮仕様・課題・不明点

- D&Dは利用者の提示した「指定範囲へドロップ」案を採用した。テクスチャモード中のキャンバス全面受け付け、
  ドロップ直下オブジェクトの自動選択・ハイライトは未実装。既存の画像追加と混同しない専用領域を初期仕様とする。
- 画像置換時は中央・全面・繰り返しなし・等倍へ初期化する。以前の画像の配置を引き継ぐかは今後検討する。
- 画像設定の数値入力ダイアログは追加せず、配置行のクリックとキャンバス直接操作を初期UIとする。
  角度と倍率の独立したキャンバスハンドル、細かい数値入力、キーボード操作は今後のUI検討対象。
- 線では外接範囲に配置した画像を線の輪郭で切り抜く。経路に沿った画像変形、線幅方向専用の貼り付けは未実装。
  フィルターの輪郭・影色にはテクスチャを設定しない。画像レイヤー自体の画像置換とは別機能である。
- アニメーション画像は静止画像として扱う。連番・動画・複数素材ライブラリー・色混合・UV編集は対象外。
- 暫定上限はファイル32 MiB、各辺16384px、合計64メガピクセル。倍率0.01～100倍、中心オフセットは
  オブジェクト幅／高さの±1000倍とする。今後、実データの負荷と操作性から見直す。
- 現状は描画・ガイド計算時にBase64復号と画像生成を行う。多数オブジェクトや大画像での性能評価、
  ランタイム解放と競合しない画像キャッシュ、JSON内で同じ画像を共有する素材テーブルは今後の課題。
- 他の塗りモードへの切替中も画像をメモリー上へ保持するが、JSONへ保存するのは現在有効な塗りモードのみ。
  別モードのまま保存・再読込した後の未使用画像の復元は未対応（従来のグラデーションと同じ扱い）。
- Windows Explorerからの実ドラッグ、AviUtl2編集ウィンドウ上のドロップ受け取り、150%／200% DPI、
  小さい属性パネルの使い勝手、極小画像ハンドル、内部グループ編集の実機確認を残す。

### 構成と検証

- モデルはCore/Model/ScreenLayoutTextureStyle、復号・画像配置はRendering/Paint/ScreenLayoutTextureRenderer、
  JSON検証はPersistence/ScreenLayoutTextureJsonへ分離した。画像UIとOLE受け口は
  ObjectProperties/Color/Texture、直接操作はEditor/Interaction/Textureに分類した。
- ScreenLayoutTextureTestで全4配置×3繰り返しのJSON往復と画素一致、透明余白、不正画像・ゼロ倍率の拒否と
  既存Document保持、複製、移動・拡縮・回転のUndo／Redo、ロック、グループ描画、モード別UIを確認する。
- ScreenLayoutTextureDropTestでWindowsのCF_HDROPデータ、領域外拒否、複数ファイル拒否、選択対象への
  一括適用とUndo／Redo、元ファイル削除後の保持、読込失敗、作成既定への継承を確認する。
- ScreenLayoutPluginRenderParityTestにテクスチャのパスを追加し、各レイヤー型のAviUtl2映像コールバックと
  共通描画のRGBA一致を確認する。既存のグラデーション・フィルター・作成・グループ関連テストも通過した。
- 完成時は関連内部テスト15本が通過し、テストのコンパイル警告・エラーもなかった。Win64のアプリ／プラグイン
  Debug／Release計4構成を通常出力先でビルドし、警告0・エラー0だった。プラグインの両構成は
  Post-buildによるAviUtl2用auf2とsk4d.dllの配置も成功した。
- 完成時の肥大化点検では、テクスチャ固有ユニットは39～269行で責務分割を維持していた。
  739行だった共通カラーピッカーから、塗り方式ボタンの描画・当たり判定を
  `ScreenLayoutPaintModeSelector`、単色・グラデーション点のプレビューと点操作を
  `ScreenLayoutColorTargetSelector`へ分離し、557行まで縮小した。
- `Source\Core\Geometry`は共通処理を直下、図形処理を`Shape`、文字処理を`Text`へ分類した。
  `Source\Editor\Interaction`は共通入力を直下、図形、文字、フィルター、グループ、グラデーション、
  テクスチャ、自由変形を各サブフォルダーへ分類した。`Source\Shell`はメイン画面とダイアログを直下、
  コンテキストメニューを`Menus`、上部操作列と属性ツールバーを`Toolbars`へ分類した。

## グラデーション拡張（2026-09-06完成）

### 実装済みの範囲

- 線形、放射（楕円）、矩形、円形（角度）、線方向、線幅方向の6種類を共通カラーピッカーで選択する。
- 始点・終点・全中間点の色と不透明度を編集できる。不透明度は0%が透明、100%が不透明である。
  レイヤー全体のOpacityとは別に保持し、描画時に乗算する。グラデーション点選択時のスライダーは
  点の不透明度を編集するため、既存のレイヤー全体のOpacityを変更しない。
- 色プレビューは点の不透明度を市松背景へ合成する。クリックで点選択、Ctrl+クリックで点追加、
  右クリックで中間点削除を行う。新しい点には、その位置の補間色と補間不透明度を採用する。
- 線形のキャンバスガイドでは既存の端点移動、線全体移動、クリック追加、右クリック削除を維持する。
  放射・矩形・円形では始点を中心、終点を主半径端として操作する。中心移動は半径・方向を維持する。
  主半径端で半径と角度を、副軸ハンドルで縦横比を直接変更する。入れ子グループの単一子選択でも使用できる。
- 座標ボタンと数値ダイアログは利用者の指示で削除した。中心、半径、角度、倍率はキャンバスで操作する。
  半径と角度は2点から導出し、同じ情報を別フィールドへ二重保存しない。
- 種類欄はグラデーション選択時だけ表示し、単色時はパネルを32px縮める。
  閉状態の背景・文字・矢印とドロップダウン項目をダークテーマで描画する。
- ガイドは白い外縁と黒い中心線、点は白黒の二重枠で描く。選択点には追加の輪を付ける。
  白背景・黒背景の両方を内部テストの画像で確認済みである。
- 点の色、不透明度、キャンバスの連続ドラッグをそれぞれ1回のUndo／Redoにまとめる。
  種別、座標の一括変更、点追加・削除も履歴へ保存する。複製したスタイルの中間点変更は元へ波及しない。
- 通常描画、単体／入れ子グループのサムネイル、AviUtl2出力は共通描画を使用する。
- version 15 JSONのpaint.typeはlinearGradient、radialGradient、rectangleGradient、sweepGradient、
  alongStrokeGradient、acrossStrokeGradientを使用する。startOpacity、endOpacity、aspectを保存し、
  従来の線形JSONでこれらが省略されている場合はすべて1として読む。
  旧実行ファイルは新種別を読み込めない。新種別をlinearGradientとして偽装して保存しない。
- JSONは塗りも事前検証し、不正種別や重複点IDなどをDocument置換前に拒否する。
  同時に、JSON読込用レコードの非管理フィールドが未初期化だった問題を修正した。

### 確定仕様・既知の制約

- 「放射」は中心から外側への距離、「円形」は主軸・副軸で正規化した時計回りの角度（1周）と仮定した。
  円形の始終色が異なる場合は0度／360度に継ぎ目ができる。名称と継ぎ目の補間方式は実機確認後に再検討する。
- 矩形は主軸・副軸方向の距離の最大値で補間する。副軸倍率1では正方形になる。
  放射・矩形・円形の種類へ切り替えると、中心(0.5, 0.5)、半径端(1, 0.5)、副軸倍率1へ初期化する。
  単なる選択変更や再表示、JSON読込では編集済みの中心を上書きしない。種別別の座標保持は今後検討する。
- 円形の中間点ガイドは半径線を0～1の編集用定規として共用する仮UIである。点の比率は実際の描画では
  0～360度に対応する。円周上へ点を並べる専用UIは今後の検討事項とする。
- 線方向・線幅方向は中心経路を等距離64区間で近似し、画素から最も近い線分で色を決める仮実装である。
  線方向は経路の始点0／終点1、線幅方向は進行方向の左側0／右側1を固定範囲とする。
  Path（直線・ベジェ）、円弧、四角／角丸四角／楕円の輪郭線へ接続し、既存の破線と線端描画を維持する。
- 鋭角、自己交差、密に折り返す曲線では、最寄り線分が切り替わる境界に色の段差が生じ得る。
  同距離では先の区間を採用する。閉経路は開始位置に継ぎ目を持つ。適応分割、頂点を跨ぐ補間、
  精密な線幅方向座標、重なりの優先順位は正式化前に検討する。
- 経路参照は1レイヤーの先頭輪郭を対象とする。現在接続している線レイヤーはいずれも1輪郭だが、
  将来複数輪郭の線を追加する場合は輪郭ごとの距離と継ぎ目の仕様が必要である。
- 線方向・線幅方向ではキャンバスの塗りガイドを無効にし、点編集は色プレビューから行う。
  線以外にこの種別を指定した場合は線形描画へフォールバックする仮仕様である。
  経路上の範囲指定、反転ボタン、繰り返し／ミラー、開始位置変更は今後の拡張とする。
- 線の近似探索は1画素あたり最大64区間を調べる。大量の太線や大きな出力での性能測定、
  探索削減、シェーダー再利用は今後検討する。線形は従来のSkia線形シェーダーを使用する。
- 今回の対象はレイヤーの共通PaintStyleである。縁取り・影フィルター自身へのグラデーション、
  パターン、テクスチャ、メッシュはこの実装に含めていない。
- 0半径は終点色・終点不透明度で描画する。副軸倍率は0.01～100へ制限する。
- 高DPI、ドック縮小、複雑な自己交差線の表示品質は、今後の通常回帰確認で継続して確認する。

### 線方向・線幅方向の必要性（UI修正後の検討）

- 利用者の「縦方向」は、現在のUIにある「線方向」を指すものとして検討した。
- 線方向は始点から終点まで経路長に沿って色を変える。直線では線形と重複するが、
  曲線・折れ線に沿った色変化や両端フェードを通常の線形だけで再現することはできず、残す価値がある。
- 線幅方向は進行方向と直交する断面の色を変える。中央を明るくした線、両縁を透明にする線、
  リボンのような立体感に用途があるが、一般的な配置編集では優先度が低い。
- 両者は縦／横の違いではなく、経路に沿う方向／経路を横切る方向の違いである。
  将来的には図形共通の種類一覧から分けて線専用設定へ移し、線幅方向は詳細設定に置く案を推奨する。
- 今回は必要性の検討までとし、2種類の機能・保存形式は削除していない。

### 検証

- ScreenLayoutGradientExtensionTest: 全6種、点追加時の不透明度補間、スナップショット独立性、
  JSON往復と描画一致、不正JSONによるDocument破壊防止、複製、入れ子サムネイル、
  折れ線・3次ベジェの線方向／線幅方向、半径0を確認する。
- ScreenLayoutGradientInteractionTest: 既存の線形操作に加え、中心移動、副軸変更、Undo、グループ内選択を確認する。
- ScreenLayoutFilterColorPickerTest: 始点・中間点・終点の不透明度、1回のUndo／Redo、全体Opacity維持、
  種別を失わないRefreshと従来フィルター操作を確認する。160×273pxの表示画像をテスト出力先へ生成する。
- ScreenLayoutPluginRenderParityTest: 従来の単色と全6種のグラデーションを使ったDocumentについて、
  AviUtl2映像コールバック経由と共通描画の合成結果が完全一致することを確認する。
- 既存のFilterRender、FilterJson、CreationPaintStyle、GroupNavigation、MainFormCreationも成功した。
- ScreenDesignMakerとSYNC_ScreenLayout_FilterのWin64 Debug／Releaseは警告0・エラー0でビルド済み。
  プラグインとsk4d.dllのPost-build配置も成功した。

## グループ編集中のレイヤー一覧方針

- 一覧はDocument最上位を維持し、開いているグループの子孫を親行の下側へ表示する。行の横位置で
  階層を表現する方式は使用せず、展開範囲を囲む左側の範囲線で所属を判別する。
- グループはダブルクリックでその場に展開・折り畳みし、入れ子でも親子関係と周囲のレイヤーを一覧上で
  同時に確認できるようにする。
- 一覧の単一選択、Ctrl追加／解除、Shift範囲選択は `TVectArtEditorState` の内部選択を使用し、キャンバスと
  同じ選択集合を共有する。表示／ロック変更と同階層D&DはDocumentの変更通知とUndo／Redoを必須とする。
- 第1段階の同一コンテナ内並べ替えと、第2段階の親子間・兄弟グループ間移動まで実装済みである。
  グループの子はDocument座標を保持するため、親を変更してもキャンバス上の表示位置は変換しない。
  階層間移動は専用コマンドで挿入位置、開いている階層、選択、Undo時の所有権を一括管理する。
- 第3段階として、一覧で複数選択済みの行からドラッグを開始した場合は選択を1件へ縮めず、選択集合を
  積層順のまままとめて並べ替え、グループへ入れる、親へ出す操作へ対応する。単体と複数の移動は
  `TScreenLayoutReparentLayersCommand` に統一し、同じ所有権移動処理を重複させない。
- 第4段階として、ドラッグ中にグループ行の中央を700msポイントすると、その場所で子を自動展開する。
  展開された子グループを続けてポイントできるため、深い入れ子階層へ1回のD&Dで移動できる。
  移動せずにドラッグを終了した場合は開始時の展開階層へ戻す。
- グループの開閉で子行数が変化した場合は、開閉対象のグループ行を基準にスクロール量を補正する。
  画面端では有効なスクロール範囲へ丸めるが、それ以外では開閉前後のグループ行のY位置を維持する。
- 展開後の長い一覧でも1回のD&Dを継続できるよう、ドラッグ位置が一覧の上下端28pxへ入った場合は
  50msごとに12px自動スクロールする。スクロール中も行間判定とグループのホバー展開を更新する。
- 状態列上部の `▸`／`▾` とグループ行は、どちらもダブルクリックで開閉する。開閉操作は
  行選択やD&D開始と分離し、1クリックでは意図せずグループを閉じない。

## レイヤーサムネイル描画の共通化方針

- レイヤー一覧では、単体レイヤーだけでなく、グループ化および入れ子グループ化した後も、子孫の
  実際の形状、配置、回転、色、透明度、線幅、線種を正しく合成してサムネイルへ表示する。
- 通常キャンバスとレイヤーサムネイルでオブジェクト別の描画処理を重複させない。描画対象範囲、
  出力サイズ、クリップ、サムネイル用最小線幅などの違いは描画コンテキストで与え、同じレイヤー
  描画ルートを使用する。
- レイヤーモデルは描画APIへ依存させない。図形固有のPath生成と幾何処理はGeometry／Rendering側へ
  置き、共通レイヤーレンダラーが型に応じて使用する。
- グループサムネイルは子孫レイヤーを再帰的に展開し、共通レンダラーでオフスクリーン画像へ描画する。
  新しいレイヤー型を共通レンダラーへ追加すれば、通常表示、単体サムネイル、グループサムネイルへ
  同時に反映される構成とする。
- 移行はオブジェクト種別ごとに行い、通常キャンバス、単体レイヤー、グループ、入れ子グループで
  表示を確認する。移行確認済みの旧サムネイル専用描画は順次削除し、全種類の移行後に残存する
  型別描画と不要な補助処理を検索して削除する。
- 通常キャンバスとレイヤーサムネイルは、レイヤー配列と論理表示範囲を受け取る共通Skia描画へ
  移行済みである。レイヤー一覧側は共通描画のRGBA結果を市松背景へ合成し、DocumentのRevision単位で
  キャッシュする。旧GDI／Direct2Dのオブジェクト別サムネイル描画は削除済みである。
- Rectangle、角丸Rectangle、Ellipse、Text、Rectangle系の線、Arc、楕円弧図形、Path、Shape、Imageで、
  通常描画が生成され、単体、グループ、入れ子グループのサムネイル結果が一致することを内部テストで
  確認済みである。

## 通常テキストの配置・変形方針

- 通常テキストは指定矩形内へ文字を組版するオブジェクトとし、文字列、フォント、文字サイズ、字間、
  行間、横書き／縦書き、テキスト枠、9方向配置、変形モード、X／Y方向スケールを保持する。選択時は
  他の矩形系オブジェクトと同様に、移動、回転、8方向ハンドルによる編集を行う。
- 字間と行間は固定pxではなく文字サイズに対する比率として保持する。文字サイズ変更や枠フィットへ
  追従させ、モード切替時の崩れとpx値への焼き直しによる累積誤差を避ける。これらは文字レイアウト
  情報とし、枠の表示変形とは分離する。
- テキストの変形方式は「縦横比を維持した均等拡縮」と、組版済みテキストブロックへX／Y別々の
  スケールを適用する「全体枠フィット」の2種類とする。新規テキストは均等拡縮を初期値とする。
  各テキストは現在の変形方式を保持し、修飾キーなしのドラッグでは保持中の方式を使用する。
  選択ツールのCtrlハンドル操作は共通の自由変形を優先する。Shiftはそのドラッグの縦横比を維持する。
  既存データに保存された全体枠フィット／均等拡縮は、通常ドラッグ時の方式として引き続き保持する。
- 文字自体の縦横比と文字サイズを維持したままテキスト枠だけを変更し、枠に応じて折り返しと行配置を
  再計算する「通常レイアウト」は使用を保留する。
- 横書きと縦書きの処理は、文字が並ぶ主進行方向と、改行または改列で進む副進行方向へ抽象化する。
  横書きは主方向を左から右、副方向を上から下とし、縦書きは主方向を上から下、副方向を右から左とする。
  フィット処理も主進行方向へのフィットとして可能な範囲で共通化する。
- テキスト枠内の配置は、水平方向の左／中央／右と垂直方向の上／中央／下を組み合わせた9方向を持つ。
  通常レイアウトと各フィット方式から独立して保持し、モードを切り替えても中央などへ強制変更しない。
  全体枠フィットでは最長行または最長列の配置差が小さくても、短い行／列と余白の配置へ反映する。
- 四隅、左右辺、上下辺のどのハンドルでも現在の変形モードに従う。均等拡縮では反対側のハンドルを
  基準にX／Yを同じ倍率で変更し、全体枠フィットでは操作した方向だけを独立して変更する。
- 持続する変形モードは、選択枠付近の小アイコン、ステータス、オブジェクト情報、ドラッグ開始時表示の
  いずれかでユーザーが確認できるようにする。
- 字間と行間は8方向の変形ハンドルとは役割を分け、専用ハンドル、ドラッグ中の数値、補助ガイドにより
  キャンバス上から直接調整する。横書きでは横方向を字間、縦方向を行間とし、単一行では行間ハンドルを
  表示しない。縦書きでは縦方向を字間、横方向を列間として扱う。
- 縦書きは横書きを単純に90度回転したものとせず、句読点、括弧、長音符、英数字、縦中横、縦組み字形、
  縦書きメトリクス、禁則処理を考慮して実装する。
- 内部データは、基本文字サイズ、字間比率、行間比率、9方向配置、横書き／縦書きを「文字レイアウト
  情報」として、X／Yスケールとフィット方式を「表示変形情報」として別々に保持する。枠フィット後の
  見た目を新しい文字サイズ、字間、行間へ焼き直さず、モードを往復しても元の値を失わない構造とする。

## 今後追加予定の機能

### 基本設計と既存機能の拡張方針

- ScreenDesignMakerは画像そのものの本格編集ではなく、画像、文字、図形、グループの配置、装飾、変形、
  合成によってサムネイル、ロゴ、画面レイアウトを作る軽量デザイン環境とする。専用オブジェクトや
  個別設定画面を増やしすぎず、図形、線、文字、Path、塗り、マスクなどの共通機能を組み合わせる。
- 四角、楕円、多角形などは固有パラメーターを保つ編集しやすいプリミティブとして保持し、論理演算や
  図形化が必要な時だけ、直線、3次ベジェ、複数外周、穴を持つ共通の閉じたPathへ変換する。
- 単線、連続線、ベジェ曲線は同じ開いたPath系列とし、単線は2頂点だけを持つ特殊形として扱う。
  線のアウトライン化では中心線から線幅の半分を両側へ展開し、曲線、線結合、始終端形状を含む外周を
  生成して閉じたPathへ正規化する。急曲線や太線による自己交差も図形として正規化する。
- 閉じた図形の加算、減算、AND、XORに加え、結合、交差、分離を共通のPath演算として拡張する。
  将来は閉じたPathの作成中に、描いた図形を選択中の図形へ即座に加算または減算する描画モードも検討する。
- 実装済みの非破壊フィルタースタックを基礎とし、発光、色調整、変形などを追加する。フィルターの
  適用順によって結果が変わる性質と、オブジェクト単位・グループ単位で同じ機構を使う方針を維持する。

### 文字機能の拡張

- 文字パスは、実装済みの上下左右の接触面、パス追従、接線方向、衝突判定、文字単位の移動・拡縮を
  維持しつつ、通常テキストと可能な範囲でモデルと処理を共通化する。将来的には別オブジェクト種別ではなく、
  同一文字オブジェクトの表示スタイル差として扱う構成を検討する。文字パス選択中は通常の9方向配置に
  代えて上下左右の接触面アイコンを表示し、ツールチップも「パス上側」など通常の文字揃えと区別する。
- 文字パスへパスオフセット、進行方向反転、文字向き反転、追従ON／OFFを追加する。衝突回避には
  自動処理ON／OFF、最小余白、自動字間補正を設け、当面は回転した文字セル同士で判定し、字形輪郭同士の
  精密判定は将来拡張とする。
- 縦書きは、縦方向の文字進行、右から左への改列、縦方向フィット、字間、行間、文字配置、文字パスとの
  組み合わせを実装する。横書きと共通化できる処理を主進行方向・副進行方向へ抽象化しつつ、縦組み字形や
  句読点、括弧、長音符、英数字、縦中横、禁則処理などは縦書き専用処理で扱う。
- 文字を字形のベクター図形へ分解する直前に、フォントの利用規約確認を促す警告を表示する。警告は
  フォント単位の初回を基本とし、「このフォントについて今後表示しない」を選択できるようにする。
  フォントファイルまたはバージョン変更時の再警告を検討し、分解後の図形には元フォント名などの
  追跡用メタ情報を保持する。警告文は利用禁止を断定せず、利用者による規約確認を促す内容とする。

### 色表現とフィルターの拡張

- 図形の塗り、文字、線、縁、影など、色を持つオブジェクトとフィルターへ共通の色表現を導入する。
  ベタ塗りだけでなく、グラデーション、パターン、テクスチャを同じ仕組みから選択できる構造とし、
  オブジェクト種別やフィルターごとの個別実装を避ける。
- 塗り方式は「なし」「ベタ塗り」「グラデーション」「パターン」を基本とする。グラデーションは
  フィルターではなく塗りそのものとして扱い、線形、放射、円形などの種別、始点、終点、角度、中心、
  半径、複数の色停止点と各位置・色・透明度を保持する。選択中は種別に応じた始終点、中心、半径を
  キャンバス上の専用ガイドから直接編集する。
- パターンの実装内容と仮仕様は上記「パターン機能（2026-09-06完成）」へ統合した。画像塗りは完成済みの
  テクスチャとして分離し、ノイズ、スクリーントーン、メッシュグラデーションは将来候補とする。
- 線にも線方向・線幅方向のグラデーション、両端フェード、中央強調、複数色、繰り返しを適用できる
  ようにし、専用の飾り罫オブジェクトを増やさず共通の線表現から構成する。
- 縁取りフィルターには縁ごとのX／Yオフセットを追加し、回転後も本体との相対位置を維持する。
  多重縁取りでは各フィルターが独立したオフセットを持つ。
- 縁取り、影など明確な輪郭を持つフィルターについて、生成結果だけを独立した閉じたPath図形へ変換し、
  通常図形と同じ編集を行えるようにする。輪郭を一意に決めにくいぼかしは図形化の対象外とする。
- 立体・押し出しフィルターは、元の図形または文字から指定方向へ厚みを生成する。必要に応じて結果を
  図形化し、自由変形や回転などの通常編集へ移行できるようにする。奥行き、押し出し方向、正面色、
  側面色、明度、複製ずらし、連続複製を設定候補とし、立体部分だけの分解にも対応する。
- ベベル／エンボス系は凸／凹、深さ、幅、光源方向、強度、ハイライト色、影色を設定候補とする。
- ライティングはキャンバス上の光源形状を直接操作する方式とし、円、楕円、四角、線を候補にする。
  光源は位置、範囲、強度、色、ぼかし、方向を持つ。AviUtl2の既存フィルターを参考にしつつ、
  ScreenDesignMakerで必要な項目へ簡略化する。
- 立体表現用の影は自動生成と手動分離を選べるようにし、落ち影、接地影、硬い影、ぼかした影、色、
  透明度、光源方向を共通設定として扱う。

### 図形・線・画像の拡張

- 図形の自由変形、左右・上下反転は実装済み。フィルターから分離した影なども自由変形、回転、
  位置調整できるようにし、奥行き方向への引き伸ばしや斜め変形による立体表現へ利用する。
- 追加する基本図形の候補は、ドーナツ、中抜き四角、それぞれの開始角・終了角を指定した部分図形、
  頂点数を指定できる正多角形および星型とする。類似図形は可能な範囲で共通パラメーター化する。
- 幾何的に自然に対応づけられる種類に限り、線から図形、図形から線への相互変換を実装する。
- 線の始点と終点へ、線端形状とは独立した末端装飾として2本線または塗りつぶし三角形の矢印を
  個別指定できるようにする。矢印サイズも線幅とは別の設定として保持する。
- 通常図形へ「通常」「マスク」「クリッピング」の役割を付与する。クリッピングは色や不透明度を
  使わない二値領域、マスクは図形の最終アルファを利用する濃淡領域とし、どちらも反転可能にする。
  適用先は原則として直下の1レイヤーまたは1グループとし、複数対象はグループ化で扱う。レイヤー一覧では
  `M`／`C`など役割を判別できる表示を付け、画像にも円、斜め、任意Path、グラデーション、パターンの
  マスクまたはクリッピングを適用できるようにする。
- 画像と閉じたPathの加算、減算、交差、反転結果を一度だけ画像アルファへ適用する確定型切り抜きを
  実装する。元の参照画像ファイルは書き換えず、加工結果を内部ビットマップまたはキャッシュとして保持し、
  ライブなマスク参照は残さない。再生成へ備えて元画像パスと使用した切り抜き情報をメタ情報として残す
  方式を検討し、確定後は移動、拡大縮小、回転のみを基本操作とする。

### 手書き線と配置補助

- WacomなどWindowsペン入力の筆圧を線幅へ変換し、一筆を可変幅の開いたPathとして保持する第1段階は
  実装済み。今後は幅点の直接編集、筆圧カーブ、筆先形状、平滑化設定と、閉じたPath図形への変換を行う。
- 不可視グリッドは、ズーム率に応じて画面上の間隔が過密にならない
  `1／2／5×10のn乗` 系列の論理刻みを自動選択する。内部座標は高精度のまま保持し、操作中だけ吸着する。
  通常オブジェクト移動と図形作成への第1段階は実装済みである。
- グリッドより優先する意味的な候補のうち、他図形とキャンバスの辺・中心、水平・垂直一致は実装済みで、
  吸着中の水平・垂直ガイドと対象外接枠を表示する。編集中オブジェクト自身の別頂点との水平・垂直一致も
  実装済みである。今後は他図形の個別頂点、他図形との平行・直交、ガイド角度、中心マーク、
  対象頂点の強調へ拡張する。
- 通常操作は意味的な候補へ賢くスナップし、Shiftは水平・垂直・代表角度、縦横比、回転刻みなどの
  制約を強化する。Altによるスナップ一時解除と、作成時のShift水平・垂直拘束は実装済みである。
  直線、Path、Shape、文字パスの次頂点は直前の点を基準に30度系・45度系へ近接吸着し、
  吸着中は2点間をスナップ色の点線で示す。Altではこの角度吸着も解除する。
  回転中の主要角度30度系・45度系への近接吸着、Shiftによる15度刻み、Altによる吸着解除、通常リサイズ、
  開いたグループの子移動まで実装済みである。画像の四隅リサイズ、
  開いたグループの子リサイズ、角度ガイドなど、未対応の操作種別へ同じ規則を順次適用する。

### 構図ガイド・整列

- スポットライト表現は、背景減光、周辺暗化、円・楕円・任意形状のハイライト、境界ぼかし、発光、
  明るさ補正、背景の彩度低下・ぼかしを、マスク、透明度、色調整などの共通機能の組み合わせで実現する。
- 日の丸構図向けに中央円、同心円、放射状ガイドを設け、円周上への均等配置、接線方向の回転配置、
  開始角、時計回り・反時計回りを指定できるようにする。阿吽型など専用処理の必要性が低い構図は
  ガイドまたはテンプレートとして提供する。
- N分割構図は1～9など任意分割数、縦・横・縦横同時、分割比率を扱い、分割線、交点、領域へ
  スナップできるようにする。数字キー1～9から分割数を指定できる操作を候補とする。通常時はキャンバス
  内部を覆わず、上下左右の外側へ短い分割マーカーだけを表示し、内部ガイドはスナップ中または
  明示的に要求された時だけ表示する。
- 複数選択またはグループに対し、左右・上下・水平中央・垂直中央揃え、水平方向・垂直方向の均等配置、
  オブジェクト間隔の均等化を実装する。キャンバス、選択範囲、固定した基準オブジェクト、グループ内部を
  整列基準として選択できるようにする。

### アニメーション

- AviUtl2上の表示期間を「入場」「通常表示」「退場」の3区間として扱い、初期値は入退場時間0の
  瞬時表示・瞬時消失とする。入場と退場は独立指定とし、フェード、拡大・縮小、移動、回転を
  基本動作にする。表示中には継続的な拡大、移動、揺れ、回転を設定できるようにする。
- 時間はフレーム数指定と表示期間全体に対する相対割合指定を候補とし、オブジェクト長変更時の追従方法を
  選択できるようにする。イージング、プリセット、グループ内アニメーション、フィルターとの適用順、
  変形フィルターと座標アニメーションの関係を詳細設計する。

### プロパティUIと制作補助

- オブジェクト情報は種類別の固定画面ではなく、塗り、線、文字、Path編集、矢印、マスク、
  クリッピングなどのCapabilityに応じて小さな共通パネルを組み立てる。右側の縦スクロール領域へ
  必要なパネルだけを固定順で積み、折り畳みと再利用に対応し、選択変更ごとの破棄を避ける。
- キャンバス上で操作可能なグラデーション、フィルター範囲、光源、配置などは数値入力より直接編集を
  優先し、数値欄は正確な補正と再現のために併設する。
- サムネイル制作補助として、セーフエリア、余白ガイド、背景と前景のコントラスト確認、色数制限・
  配色補助、集中線、構図テンプレート、縮小プレビュー、小サイズ時の可読性確認、完成画像比較、
  ランダム配置補助を候補とする。抽象表現や手書きロゴは専用エンジンを設けず、自由線、文字図形化、
  Path編集、塗り、フィルター、テンプレートの組み合わせで実現する。

### 将来機能の共通設計方針

- 色表現は全オブジェクトと全フィルターで共有し、フィルター結果は必要に応じて通常図形へ変換する。
- 変換後の図形には反転、自由変形、回転、位置調整などの共通編集機能を適用する。
- 通常テキスト、文字パス、縦書きは、可能な範囲で同一文字モデルのスタイル差として管理する。
- マスク、クリッピング、フィルターなどの可逆処理と、画像切り抜きなどの不可逆処理をUIとデータの
  両方で明確に区別する。
- 参照画像は可能な限り元ファイルを保持し、加工が必要な場合だけ内部キャッシュまたは一時実体を生成する。
- サムネイル制作に必要な表現力を優先し、長文組版、3D、画像編集は用途以上に複雑化させない。

## プロジェクト構成

- Delphiプロジェクトはリポジトリルートの `SYNC_ScreenLayout_Filter.dpr` と
  `SYNC_ScreenLayout_Filter.dproj` を使用する。
- `ScreenDesignMaker.dpr` と `ScreenDesignMaker.dproj` は、同じ編集画面をAviUtl2なしで
  起動・デバッグするWin64 VCLアプリである。AviUtl2固有のユニットは参照しない。
- ScreenDesignMakerだけは「ファイル」メニューからDocumentのJSON読込・保存ができる。
  ファイル選択にはVCLの新しいダイアログ `TFileOpenDialog` と `TFileSaveDialog` を使用する。
- ScreenDesignMakerの成果物は `Win64\ScreenDesignMaker\Debug` または
  `Win64\ScreenDesignMaker\Release` へ出力し、`sk4d.dll` も同じ場所へコピーする。
- プロジェクトから階層を上がる相対参照は使用せず、`Lib` と `Source` 以下への下方向の参照だけを使用する。
- 画面レイアウトプラグイン固有のホスト処理は `Source\PlacementPlugin` に配置する。
- 共通UI、モデル、描画、永続化、補助コントロールは、用途に応じて `Source` または `Lib` 以下へ配置する。
- `Source\ObjectProperties` 直下には属性パネル全体の構成、共通コマンド、選択解決だけを置き、
  色関連は `Color`、フィルター関連は `Filters`、形状・線属性は `Geometry` へ分類する。
- `Source\Core\Commands` 直下には共通履歴、フィルター、描画スタイルのコマンドだけを置き、
  階層操作は `Layer`、図形編集は `Shape`、文字編集は `Text` へ分類する。
- Skiaの描画スタイルとグラデーションシェーダー構築は `Source\Rendering\Paint` に置き、
  Document全体の描画順やフィルター合成を担当するRendererから分離する。
- `Source\Core\Model` はDocument、編集状態、履歴、Contextを、`Source\Core\Geometry` は
  UI状態を持たない図形計算を配置する。編集入力は `Source\Editor\Interaction`、新規作成処理は
  `Source\Editor\Creation` に配置し、`Source\Editor` 直下にはキャンバスとWorkspaceだけを置く。
- JSONの公開窓口は `ScreenLayoutDocumentJson` とし、書き込みを
  `ScreenLayoutDocumentJsonWriter`、検証と読み込みを `ScreenLayoutDocumentJsonReader` が担当する。
- 新規レイヤー名の採番は `ScreenLayoutLayerNaming`、キャンバス操作の状態を持たない距離・外接範囲
  計算と選択枠のリサイズ計算は `ScreenLayoutInteractionGeometry` が担当する。
- Shape論理演算は `ScreenLayoutShapeBooleanGeometry` がSkia Pathから輪郭への変換、
  `ScreenLayoutShapeBooleanCommands` がUndo／Redo、`ScreenLayoutShapeBooleanOperations` が
  選択状態と演算手順の調整を担当する。
- 頂点列の幾何処理は、開いたPathを `ScreenLayoutPathOperations`、閉じたShape輪郭を
  `ScreenLayoutShapeOperations` が担当し、DocumentやUI状態には依存しない。
- PathとShapeのベジェアンカーおよびベジェ制御点は表示矩形の周囲にも当たり判定を持ち、同種の点の
  範囲が重なる場合は最寄りを選ぶ。拡張範囲をホバーした時点で移動カーソルを表示し、ドラッグ可能な
  ことを示す。制御点の拡張範囲とアンカーの表示範囲が重なる場合はアンカーを優先する。鋭角アンカーは
  文字パス本体などのドラッグ領域を不必要に奪わないよう、従来の表示範囲で判定する。
- キャンバス入力は `ScreenLayoutCanvasInteraction` が選択、共通変形、操作の振り分けを担当し、
  `ScreenLayoutShapeInteraction` がShapeの頂点選択、区間分割、頂点削除、種別変更、
  ベジェ制御点の操作と表示用幾何を担当する。
- 文字パス上の文字単位の選択、拡張当たり判定、個別移動・拡縮、Undo確定は
  `ScreenLayoutTextPathCharacterInteraction` が担当し、Canvasは入力方式の振り分けだけを行う。
- フィルター効果枠の描画、当たり判定、直接操作、Undo確定は
  `ScreenLayoutFilterInteraction` が担当し、Canvasはマウスイベントと描画先だけを委譲する。
- フィルターUIの外枠とコマンド連携は `ScreenLayoutFilterFrame`、一覧固有の描画、D&D、
  有効スイッチ、主要値スライダーは `ScreenLayoutFilterListControl` が担当する。
- 文字入力のWindows IME受け取り、UTF-16／CRLFを壊さない選択・カーソル操作、折り返し行の
  カーソル位置計算は `ScreenLayoutTextEditing` が担当し、CanvasはDocument反映と表示更新を担当する。
- 右上属性ツールバーは `ScreenLayoutLineToolbar` が表示と入力状態を担当し、線属性のDocument反映と
  履歴構築は `ScreenLayoutLineToolbarOperations`、文字属性は `ScreenLayoutTextToolbarOperations` が担当する。
- このリポジトリ固有の共有ユニット名には `ScreenLayout` 接頭辞を使用し、分離元アプリの
  `VectArtDesigner` 接頭辞は使用しない。
- Google Skiaの文字描画ランタイムは `Lib\Skia\Win64\sk4d.dll` を使用する。
- 編集キャンバス上の線種・線端プレビュー、回転マーク、頂点種別と角丸半径の補助アイコン描画は
  `ScreenLayoutCanvasPreview` が担当する。字間・行間の双方向矢印も同じ補助描画として管理する。
- グラデーション操作は `Source\Editor\Interaction\Gradient` に分類し、入力、当たり判定、
  Undo確定を `ScreenLayoutGradientInteraction` が担当する。ガイド描画は
  `Source\Rendering\Overlay\ScreenLayoutGradientOverlay`、種類コンボは
  `Source\ObjectProperties\Color\ScreenLayoutGradientKindCombo` へ分離する。
- 文字編集中の選択範囲と新規入力ガイドは `ScreenLayoutTextEditOverlay` がGDI／Direct2D共通の
  配置計算と描画を担当し、Canvasは現在の編集状態だけを渡す。
- Windows IMEの開閉状態の退避と復元は `Lib\InputMethod\WindowsImeController` が担当し、
  Canvas固有の文字入力処理からIMM API呼び出しを分離する。
- キャンバスの文書ビットマップ、移動用の上下階層RGBAキャッシュ、ズーム中の画像再利用判定は
  `Source\Rendering\Cache\ScreenLayoutCanvasRenderCache`が担当し、Canvasは表示条件だけを渡す。

## 共通ビルドルール

- Delphi 37.0を使用し、対象プラットフォームはWin64とする。
- DebugとReleaseの両構成を検証する。
- コンパイル警告とエラーを確認し、原則として警告0、エラー0で完了とする。
- フィルタープラグインの通常ビルドは、Debug／ReleaseそれぞれのPost-build eventで生成したDLLを
  `C:\ProgramData\aviutl2\Plugin\SYNC_ScreenLayout\SYNC_ScreenLayout_Filter.auf2` として配置し、
  `Lib\Skia\Win64\sk4d.dll` も同じディレクトリへコピーする。AviUtl2がプラグインを使用中で
  上書きできない場合はビルドを失敗させ、旧版のまま成功扱いにしない。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.exe`、`.dll`などのビルド成果物はGitへ追加しない。
- 一時的な内部テストも実行ファイル名を必ず `.exe` にし、拡張子なしの成果物は起動しない。
  起動前に対象の `.exe` が存在することを確認し、Windowsの「開くアプリを選択」を発生させない。
- 日本語文字列リテラルを持つ `.pas` と `.dpr` はUTF-8 BOM付きで保存する。

## コメントルール

- コードを読めば分かる処理の言い換えではなく、目的、責務、注意点、状態や値の意味を書く。
- 古い仕様や現在の実装と食い違うコメントは、見つけた時点で更新する。
- 不要なコメント、同じ内容の重複、処理を日本語へ置き換えただけのコメントを増やさない。
- ユニット先頭には、そのユニットの目的と担当範囲を `//` で書く。
- `interface` の公開関数・手続きには、呼び出し側から見た責務、入出力、重要な副作用を書く。
- フィールドや定数の短い説明は行末へ置き、同じブロックでは `:`、`=`、`//` の位置を可能な範囲で揃える。
- レコードの各フィールドには用途または値の意味を書く。バイナリー形式に対応するレコードでは配置と型の理由も明記する。
- コメントと対象の宣言または実装の間に不要な空行を入れない。
- `var` ブロック内へローカル関数・手続きを置かず、補助処理は同じ `implementation` の独立関数へ分ける。
- `property`、`procedure`、`function` の宣言は、112文字以内なら折り返さない。
