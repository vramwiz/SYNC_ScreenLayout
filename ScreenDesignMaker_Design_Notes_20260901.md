# ScreenDesignMaker 設計検討メモ

## 1. 基本方針

ScreenDesignMaker は、Illustrator や Affinity Designer に近い考え方を持つ、
画面レイアウト向けの軽量デザインアプリとして設計する。

主に扱うオブジェクトは以下とする。

- 画像
- 文字
- 図形

画像編集そのものよりも、複数オブジェクトの配置・装飾・変形・合成を行い、
画面全体を構成する用途を中心とする。

---

## 2. 図形の基本概念

### 2.1 連続線・単線・ベジェ曲線

以下は GUI 上では異なる作成方法を持ってよいが、
内部データとしては同じ系列として扱う。

- 単線
- 連続線
- ベジェ曲線

単線は、連続線のうち2点だけを持つ特殊形とする。

各点は以下のような情報を持てる。

- 直線的な鋭角点
- ベジェ曲線用の制御点
- 必要に応じた接続形状

---

### 2.2 四角・楕円・多角形

以下は通常編集時には、それぞれ独立したプリミティブ図形として扱う。

- 四角
- 楕円
- 多角形

理由は、それぞれ固有の編集パラメータを保持した方が操作しやすいため。

例：

- 四角：幅、高さ、角丸
- 楕円：幅、高さ
- 多角形：頂点数、半径、回転

ただし、図形同士の加算・減算などを行う場合には、
共通の閉じたパス形式へ変換する。

---

## 3. 共通パス図形

図形合成のため、最終的には以下の図形を共通形式へ変換できるものとする。

- 連続線
- 四角
- 楕円
- 多角形

共通形式は、単純な直線だけの多角形ではなく、

- 直線頂点
- 鋭角頂点
- ベジェ曲線
- 複数の閉じた輪郭
- 穴となる内周

を持てる「閉じたパス図形」とする。

---

## 4. 連続線のアウトライン化

連続線は「中心線＋太さ」を持つ図形として扱う。

図形合成などで必要になった場合は、

1. 中心線を基準とする
2. 線幅の半分だけ左右へ展開する
3. 外周輪郭を生成する
4. 末端形状を生成する
5. 閉じたパスへ変換する

という手順でアウトライン化する。

曲線区間についても、ベジェ曲線の外周を生成する。

急な曲線や太い線などにより自己交差が発生した場合は、
必要に応じて閉じたパスとして正規化する。

---

## 5. 線の末端

線には始点と終点という概念を持たせる。

初期段階では、以下の末端形状を用意する。

### 処理なし

標準。

中心線の終端位置で垂直に切る。

アウトライン化すると、平らな末端になる。

### 丸

終点を中心として、半径が線幅の半分となる半円を付ける。

### 尖り

線幅の半分だけ進行方向へ突出した1点を作り、
左右の線端と接続して二等辺三角形状にする。

線幅に比例して突出量も変化するため、
太さを変更しても自然な比率を維持できる。

---

## 6. 矢印

矢印は末端形状とは別の「末端装飾」として考える。

初期段階では以下を候補とする。

- 2本線による矢印
- 塗りつぶし三角形による矢印

線そのものの尖った末端とは区別する。

始点・終点で別々の指定が可能な構造とする。

---

## 7. 図形の加算・減算

閉じた図形同士について、ブーリアン演算を可能とする。

初期実装では、専用描画モードを増やさず、

1. 2つの図形を選択
2. 加算または減算を実行

という操作を優先する。

### 加算

2つの閉じた図形を結合する。

必要に応じてプリミティブ図形を共通パスへ変換してから処理する。

### 減算

基準となる閉じた図形から、もう一方の図形領域を除去する。

例：

1. 大きな円を作成
2. 小さな円を内側に作成
3. 2つを選択
4. 減算

結果としてドーナツ型の閉じたパスを作成できる。

### 合成結果

加算・減算結果は基本的にパス図形として保持する。

1つのパス図形内には、

- 複数の外周
- 穴となる複数の内周

を持てるようにする。

---

## 8. 将来的な描画時ブーリアン

初期実装では優先しないが、
将来的には閉じたパス選択時に以下の描画モードを追加できる。

- 標準
- 加算
- 減算

標準では通常通り新規オブジェクトを作成する。

加算では、描画した図形を選択中の閉じたパスへ即座に加算する。

減算では、描画した図形を選択中の閉じたパスから即座に除去する。

内部では既存の加算・減算処理を再利用する。

---

## 9. グリッド

### 9.1 不可視グリッド

常時方眼を表示する方式は採用しない。

基本となるグリッドは「見えないグリッド」とし、
何もスナップ対象がない場合でも必ずここへ吸着する。

グリッド間隔は固定ではなく、
現在のズーム率から適切な間隔を自動算出する。

考え方としては、

- 高倍率では細かい刻み
- 低倍率では粗い刻み
- 画面上でのグリッド間隔が極端に狭くならない
- 1 / 2 / 5 / 10 / 20 / 50 / 100 など扱いやすい刻みに寄せる

とする。

内部座標そのものは高精度で保持し、
ユーザー操作時だけ不可視グリッドへスナップする。

---

## 10. スナップ

不可視グリッドの上に、より意味のあるスナップ候補を持たせる。

候補例：

- 自分自身の他頂点との水平一致
- 自分自身の他頂点との垂直一致
- よく使う角度
- 他図形の頂点
- 他図形の辺
- 他図形の中心
- 自分自身の中心
- キャンバス中央

意味のあるスナップ候補が存在する場合は、
不可視グリッドより優先して吸着してよい。

---

## 11. スナップ表示

吸着した場合は、必ず何に関係して吸着したかを表示する。

例：

- 水平一致：水平ガイド
- 垂直一致：垂直ガイド
- 中心一致：中心マークやガイド
- 角度一致：角度ガイド
- 頂点一致：対象頂点を強調

単に座標が止まるのではなく、
吸着理由をユーザーが視覚的に把握できるようにする。

---

## 12. 補助キー

### Shift

通常状態よりも制約を強くする。

操作内容によって意味を変えてよい。

例：

- 移動：水平・垂直・代表角度へ固定
- サイズ変更：縦横比固定
- 回転：一定角度刻み
- 頂点操作：代表方向への固定

### Alt

通常のスナップや制約を一時的に解除し、
自由操作を可能とする。

基本思想は、

- 通常：賢くスナップ
- Shift：制約を強化
- Alt：自由操作

の3段階とする。

---

## 13. フィルターの基本概念

AviUtl2 上で利用することを考慮し、
画像や図形への追加加工を「フィルター」と呼ぶ。

各オブジェクトは初期状態ではフィルターを0個持つ。

必要に応じてフィルターを積み重ねる方式とする。

AviUtl2 のフィルターに近い操作感を目指す。

---

## 14. 元オブジェクトとフィルターの分離

### 元オブジェクト

元オブジェクト側には最低限以下を持たせる。

- 形
- 塗り
- 線
- 基本透明度

### フィルター

追加の装飾や加工を担当する。

例：

- 縁取り
- 影
- ぼかし
- 発光
- 色調整
- 変形
- その他の追加加工

フィルター側に「塗り」は不要とする。

変形は非破壊編集との相性がよいため、
フィルターとして持たせてもよい。

---

## 15. フィルタースタック

オブジェクトを選択するとフィルターUIを表示する。

フィルターはレイヤーのように一覧管理する。

必要な操作：

- フィルター追加
- フィルター削除
- フィルター選択
- フィルター順序変更
- ON/OFF
- 各フィルターの設定編集

フィルター順序には意味を持たせる。

例：

- 変形 → 縁取り → 影
- 縁取り → ぼかし
- 影 → 発光

など、並び順に応じて結果が変化する。

---

## 16. 多重縁取り

文字装飾では多重縁取りが重要になる。

縁取りを1つのフィルターとして扱い、
複数の縁取りフィルターを積み重ねることで対応する。

例：

1. 太い黒縁
2. 細い白縁
3. 影
4. 発光

文字専用の特殊機能として固定せず、
フィルタースタックとして扱うことで拡張性を持たせる。

---

## 17. グループとフィルター

グループ単位でもフィルターを持てるようにする。

例として「Φ」を、

- 円
- 縦線

の2オブジェクトで構成し、グループ化する。

このグループ全体へフィルターを掛けることで、
円と縦線を合成した見た目全体へ加工を行える。

一方、グループは解除せず開くだけで、

- 円だけ移動
- 縦線だけ移動
- 個別サイズ変更
- 個別形状変更

などを行える。

つまり、

- 個別オブジェクトの編集性を保持
- グループ全体の描画結果へフィルター適用

を両立する。

オブジェクト単位とグループ単位の両方で
同じフィルター機構を利用できるようにする。

---

## 18. アニメーションの基本概念

ScreenDesignMaker のオブジェクトは、
AviUtl2 上である程度の表示フレーム数を持つことを前提とする。

初期状態では、

- 一瞬で表示
- 一瞬で消える

を標準動作とする。

アニメーション時間を設定した場合のみ、
滑らかな出現・消失を行う。

---

## 19. 表示期間の構造

オブジェクトの表示期間を以下の3区間として考える。

1. 入場アニメーション
2. 通常表示
3. 退場アニメーション

開始・終了のアニメーション時間が0の場合は、
従来通り瞬時に表示・消失する。

---

## 20. 入場・退場アニメーション

開始と終了は独立して指定できるようにする。

例：

- フェードイン → 通常表示 → フェードアウト
- 拡大しながら表示 → 通常表示 → 下へ移動しながら消える
- 回転しながら表示 → 通常表示 → 縮小して消える

候補となる基本動作：

- フェード
- 拡大・縮小
- 移動
- 回転

---

## 21. 表示中アニメーション

入場・退場だけではなく、
オブジェクト表示中ずっと動作するアニメーションも想定する。

例：

- ゆっくり拡大
- 徐々に移動
- 微妙に揺れる
- 継続回転

そのためアニメーションは将来的に、

- 入場
- 表示中
- 退場

の3系統へ整理できる。

---

## 22. アニメーション時間指定

オブジェクト単位で表示フレーム数を持つため、
以下の2方式を検討できる。

### フレーム数指定

例：

- 入場：10フレーム
- 退場：8フレーム

動画編集用途では直感的。

### 相対時間指定

オブジェクトの表示期間全体を0～100%として、

- 0～10%：入場
- 10～90%：通常表示
- 90～100%：退場

のように指定する。

オブジェクト長を変更した場合も、
演出タイミングを自動追従させやすい。

---

## 23. 現時点での大分類

ScreenDesignMaker の基本構造は現時点で以下のように整理できる。

### オブジェクト

- 画像
- 文字
- 図形
- グループ

### 図形

- 連続線
- 四角
- 楕円
- 多角形
- 閉じたパス図形

### 図形処理

- アウトライン化
- 加算
- 減算

### 配置補助

- 不可視グリッド
- 自己スナップ
- 他図形スナップ
- 中央スナップ
- ガイド表示
- Shiftによる制約強化
- Altによる自由操作

### フィルター

- オブジェクト単位
- グループ単位
- スタック方式
- 順序変更可能
- 非破壊

### アニメーション

- 入場
- 表示中
- 退場
- 初期値は瞬時表示・瞬時消失

---

## 24. 今後の検討候補

- アニメーションUI
- イージング
- アニメーションのプリセット
- グループ内アニメーション
- フィルターとアニメーションの適用順
- 変形フィルターと座標操作の関係
- スナップ優先順位の詳細
- 図形加減算時の選択順
- Undo / Redo 単位
