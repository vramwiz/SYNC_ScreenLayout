# SYNC_ScreenLayout 実装履歴

このファイルは、開発時点ごとの実装状況と検証内容を保管する。現在の作業判断と設計方針は [note.md](note.md)、利用者向けの機能説明は [README.md](README.md) を参照する。

- 追加機能の完成整理として、Canvasから文書画像、移動用の階層別RGBA、ズーム再利用判定を
  `ScreenLayoutCanvasRenderCache`へ分離した。自由変形のUndo／Redoは
  `ScreenLayoutLayerTransformCommands`へ分離し、入力計算と履歴復元の責務を分けた。
  Shell直下のメニュー系を`Menus`、ツールバー系を`Toolbars`、自由変形入力を`Transform`へ分類した。
  整理後に全43テストを再コンパイルして実行し、すべて成功した。アプリとプラグインの
  Win64 Debug／Release計4構成も再コンパイルし、Release版アプリと配置先プラグインを更新した。
- 自由変形済みの角丸、円弧、通常文字では、変形前座標のまま残っていた角半径、円弧角度、
  字間・行間の個別編集ハンドルを非表示にした。自由変形用の選択枠とハンドルだけを表示する。
- 通常文字をクリックだけで作成した場合、初期入力幅を超える文字列に合わせて枠を横へ伸ばし、
  Enterなしの自動折り返しを行わないようにした。ドラッグで幅を指定した文字は従来どおり折り返す。
- 通常文字のIME変換中は、Enter、矢印、Backspaceなどのキー処理をIMEへ渡すようにした。
  漢字変換をEnterで確定した際、同じキー入力で意図しない改行が追加される問題を修正した。
- 単一オブジェクトの移動中は、Document直下を下層・選択層・上層へ分けて開始時に描画し、
  以後は選択層のRGBA画像だけを移動して元の積層順で合成するプレビューへ変更した。
  閉じたPath自身を動かす場合と、重いPathを背景に置いて別オブジェクトを動かす場合の再描画を抑える。
  自由変形済みオブジェクトの移動にも適用し、ドラッグ中の外部UI通知は確定時の1回へまとめた。
  積層順を含むプレビュー画素一致、通常・変形済みShapeの実キャンバス操作、Undoを回帰テストへ追加し、
  全43テストとアプリ・プラグインのWin64 Debug／Releaseコンパイルに成功した。
- ホイールで連続ズームしている間は直前のRGBA画像を新しい表示範囲へ拡縮し、入力停止後に一度だけ
  現在倍率のベクター描画へ更新する。各ズーム段階で閉じたPathとフィルターを再描画する負荷を除いた。
- 選択ハンドルへCtrlの自由変形・せん断、Ctrl+Altの対称遠近変形、Shiftの方向制限、
  CtrlなしのShift縦横比維持・Alt中心固定を追加した。元データと表示変形を分離し、共通描画、
  選択判定、頂点操作、保存・複製、削除後の復元、文字分解・図形化、ブーリアン演算へ引き継ぐ。
  単一・複数・グループ内選択を扱い、ドラッグを1件のUndoとして保存し、Escで取り消す。
  自由変形の回帰テストを含む全43テストと、アプリ・プラグインのWin64 Debug／Releaseの
  全4構成のコンパイルに成功した。プラグイン配置はAviUtl2側のファイル使用中により未反映。
- 高DPIの単独アプリでレイヤー一覧だけが小さく表示される問題を修正した。行高、行間、余白、
  サムネイル、市松模様、表示・ロック・展開アイコンを`CurrentPPI`で拡大し、当たり判定、
  スクロール量、ドラッグ操作も同じ倍率へ揃えた。96／192 DPIの配置テスト、既存操作テスト、
  アプリとプラグインのWin64 Debug／Releaseビルドに成功した。
- 高DPIの単独アプリで、線選択時に太さスライダーがツールバー左端から欠ける問題を修正した。
  選択状態の更新時にも線用270px、文字用420pxの論理幅を`CurrentPPI`で変換する。
  192 DPIでツールバー幅、スライダー位置、数値欄との非重複を確認する回帰テストを追加した。
- 単独アプリのDPI設定を`none`からPer-Monitor V2へ変更した。Delphiが生成するプロジェクトリソースを
  `.dpr`からリンクし、Debug／Release実行ファイルの埋め込みマニフェストに`PerMonitorV2`と
  `true/pm`が含まれることを確認した。独自描画の上部操作列、選択別ツールバー、ツールパレット、
  レイヤー操作列、ダークメニューを`CurrentPPI`基準へ変更し、表示とクリック判定を一致させた。ドック幅、フォーム寸法、
  フローティング寸法は96 DPI換算で保存し、既存のversion 1設定も現在のDPIへ変換して読み込む。
  200% DPIでRelease版の画面と編集メニューを確認し、関連テスト、アプリDebug／Release、
  プラグインDebug／Releaseのコンパイルに成功した。
- 複数選択時の右クリックへ「整列と均等配置」を追加した。左・水平中央・右、上・垂直中央・下の
  6方向整列と、両端を固定して中間要素の輪郭間隔を揃える水平・垂直均等配置に対応する。
  トップレベルと開いたグループ内選択を同じ処理で扱い、移動全体を1件のUndo／Redoとして記録する。
- 独自ダークポップアップの項目描画を標準メニューに近づけ、名称を左列、ショートカットを右列、
  サブメニュー記号を右端へ揃えた。従来の空白区切りCaptionも解析し、既存メニューへ共通適用する。
- 上部ツールバーとオブジェクト右クリックの「変形」へ左右・上下反転を追加し、Shift+H／Shift+Vを
  接続した。単一選択、複数選択の共通軸、閉じたグループ、開いたグループ内選択に対応し、文字、画像、
  Path、Shape、角半径、円弧、グラデーション、パターン、テクスチャ、影を見た目ごと反転する。
  操作全体を1件のUndo／Redoとして保存し、version 15 JSONへ反転状態を保持する。専用テストを含む
  全41テストと、アプリ／プラグインのWin64 Debug／Release計4構成が通過した。
- 配布用`Setup/SYNC_ScreenLayout.zip`と再生成スクリプトを追加した。ZIPは
  `SYNC_ScreenLayout`フォルダ内にプラグイン本体と`sk4d.dll`を収録する。
  AviUtl2カタログ用JSONにはGitHub Release取得、配置・削除手順、初回`v1.0.0`と主プラグインの
  XXH3-128を記録した。展開後の2ファイルは配布元とSHA-256一致し、JSONのXXH3-128とも一致した。
- フィルターの色を選択した後、同じテキスト本体をクリックしてもフィルター色が色ピッカーへ残る問題を修正した。
  キャンバス上の本体またはレイヤー一覧の行を選び直すとフィルター内選択を解除し、文字・図形本体の色へ戻す。
  別レイヤーを選んだ場合も、フィルター所有レイヤーが現在の単一選択でなければ同じ状態を解除する。
  青いテキストと赤い縁取りの再現テストを追加し、関連4テストと全4構成のビルドを確認した。
- フィルター追加の＋から開く標準ポップアップを、共通のダークポップアップへ変更した。
  再クリック、外側クリック、Esc、非アクティブ化による閉鎖と、追加操作のUndo／Redoへ対応する。
- グラデーション種類とテクスチャ設定のフォント高を`CurrentPPI`基準へ変更した。文字とともに行高、
  余白、コンボ矢印、プレビュー内キャプション位置を拡大し、アプリ版とプラグイン版の表示倍率を揃えた。
- DPI対応ホスト内のプラグイン編集画面をGDIスケーリング用の独立したスレッドDPIコンテキストで生成し、
  96 DPI座標へ統一した。レイヤー一覧、ツール列、上部操作列の寸法差を解消する。
- 色UIを上部固定設定、中央のモード固有設定、下部固定カラーピッカーへ整理した。透明度は数値を表示せず、
  「透明度：」とスライダーを横並びにした。塗りモードを切り替えても固定領域とパネル高は移動しない。
- フィルター操作列の追加ボタンを拡大し、文字のXだった削除ボタンを線画のゴミ箱アイコンへ変更した。

## 2026-09-06 パターン機能完成

- パラメーターをアプリ共通の横型スライダーへ変更し、数値入力欄を撤去した。
  項目名とスライダーを横並びにし、数値表示を撤去した。0.1刻みのキー調整、アプリ共通の
  縦スクロールバー、連続Undo／Redoを接続した。
- 線幅などの広い値域を感度補正し、短いドラッグで最大値へ達しにくくした。スライダー上では
  マウスホイールで0.1ずつ微調整できる。
- 内蔵6種、定義別の数値、色スロットと個別不透明度、ダークテーマUI、ライブプレビューを追加した。
- 図形・文字・線の共通描画、倍率に応じたタイル生成、描画内キャッシュ、JSON保存・事前検証へ接続した。
- 複数選択、作成既定、色・不透明度・数値の連続Undo／Redoに対応した。
- モデル・描画・保存・専用UI・履歴を責務別ユニットとPatternフォルダへ分けた。
- 完成時の肥大化点検で設定コントロールからプレビュー生成・表示を分離し、パターン固有ユニットを93～269行に整理した。
- 新規2本と既存15本のテスト、AviUtl2出力の画素一致、4構成のビルドを確認した。
- キャッシュ寿命・数値制限などの仮仕様と、UI・実機確認・フィルターへの将来拡張をnote.mdへ記録した。

## 2026-09-06 テクスチャ機能完成

- 専用領域の画像D&D・ファイル選択、プレビュー、置換・解除、4種類の配置、3種類の繰り返しを追加した。
- キャンバスで中心移動と拡縮・回転ができ、複数選択への画像適用とドラッグ操作をUndo／Redoに接続した。
- 埋め込み画像と配置をversion 15 JSONへ保存し、不正データをDocument置換前に拒否する。
- 共通描画とAviUtl2出力へ接続し、画像UI、OLE、モデル、描画、JSON、直接操作を責務別ユニットへ分離した。
- 仮仕様、性能上の課題、実機確認項目をnote.mdへ記録した。
- 完成時は関連テスト15本が通過した。アプリ／プラグインのWin64 Debug／Release計4構成は警告0・エラー0で、
  AviUtl2用auf2とsk4d.dllのPost-build配置も成功した。
- 完成時の構成点検で、共通カラーピッカーから塗り方式選択UIと色・グラデーション点の操作UIを分離した。GeometryはShape／Text、
  InteractionはShape／Text／Filter／Groupと既存のGradient／Textureへ用途別に再配置した。

## 2026-09-06 グラデーションUI調整

- 座標ボタンと入力ダイアログを削除した。種類欄はグラデーション選択時だけ表示し、
  選択欄・矢印・ドロップダウン項目を暗色へ統一した。
- 放射・矩形・円形の種類への切替時は、中心をオブジェクト中央へ初期化する。
- ガイドと点の枠は白黒の二重表示、選択点は追加の輪で示す。白背景と黒背景の表示を確認した。
- 線方向は曲線追従用として有用、線幅方向は装飾用で低優先度と評価した。
  将来は線専用設定へ分離する案をnote.mdへ記録した。機能の削除は行っていない。
- グラデーションを完成扱いとした。肥大化していた操作ユニットからガイド描画を分離し、
  種類コンボも色ピッカーから独立させた。Interaction直下の関連ユニットはGradientへ分類した。

## 2026-09-06 グラデーション拡張

- 点ごとの不透明度、放射・矩形・円形（角度）、線方向・線幅方向を追加した。
- 中心・半径端・副軸の直接操作、点プレビューからの追加・削除、座標入力、Undo／Redo、
  複製、version 15 JSON、共通描画へ接続した。線経路の64区間近似などの仮仕様はnote.mdへ記録した。
- JSON読込時の塗りレコード未初期化と、不正な塗りをDocument置換後に検出する問題を修正した。
- 9本の関連内部テストが成功した。全6種のJSON往復、折れ線・ベジェ、点の不透明度の履歴、
  グループ内部編集、入れ子サムネイル、AviUtl2映像コールバックとの出力一致を含む。
- Win64 Debug／Releaseのアプリ・プラグイン計4構成は警告0・エラー0でビルドし、プラグイン配置も成功した。
- 160×273pxのカラーUI表示は確認済み。高DPI、狭いドック、実機での操作感と複雑な線の表示は未確認。

## 2026-09-05時点

- 座標系見直しの第1段階として、Document JSONをversion 14で中央原点へ更新し、その後フィルター
  スタック追加に伴ってversion 15へ更新した。`canvas.origin: "center"` を必須化し、旧versionと
  左上原点形式は読み込まない。編集座標変換、JSON往復、共通RGBA描画で
  `(0, 0)` がキャンバス／出力画像の中央になることを `ScreenLayoutCoordinateSystemTest` で確認済みである。
- AviUtl2で新規作成するDocumentは、映像コールバックが最後に使用したオブジェクト出力寸法を
  キャンバス寸法として開始する。出力寸法をまだ取得できない場合だけ背景フレーム寸法を使用し、
  保存済みDocumentを開く場合はJSON内のキャンバス寸法を維持する。
- 現行の全レイヤー型を含むversion 15 Documentについて、共通レンダラーのRGBAを不透明な入力画像へ
  合成した結果と、AviUtl2映像コールバック経由の出力が完全一致することを
  `ScreenLayoutPluginRenderParityTest` で確認済みである。文字パスは上面、下面、左面、右面の4種類を
  同じDocumentへ含め、JSON受渡し後の接触面とプラグイン出力の一致を確認する。
- プラグインから編集画面を生成する際、ドッキング用FrameはWinControlの所有者へ一時接続してから
  子コントロールを初期化し、Contextは正式なドックスロットへ接続した後に設定する。これにより
  `LayerPanelFrame`／`ObjectPropertiesFrame`が親ウィンドウを持たない生成時例外を防ぐ。
  `ScreenLayoutMainFormCreationTest`でフォームの生成と破棄を確認する。
- 編集キャンバスはズーム／パン後の表示矩形を出力範囲として扱い、範囲外を中間グレーでべた塗りし、
  四隅から8px離れた外側へ画面ピクセル固定長のトンボ線を表示する。キャンバス縁線は描画しない。
  トンボ位置は表示矩形を基準とし、ビュー内に入る部分だけを表示する。これらは編集補助表示だけで、Documentおよび
  プラグイン出力へ含めない。補助線は `ScreenLayoutCanvasGuides` に分離し、
  `ScreenLayoutCanvasGuideTest`で表示矩形に対する固定トンボ座標を確認する。この対応はいったん完了とする。
- 編集補助表示は `Source\Rendering\Overlay` 配下へまとめ、線と折れ線を
  `ScreenLayoutOverlayPrimitives`、矩形・楕円・円弧などの輪郭を `ScreenLayoutOverlayShapes`、
  操作点を `ScreenLayoutOverlayHandles` が担当する。GDI／Direct2Dの両方で
  白2pxの細い外縁へ黒または意味色の1px中心線を重ね、描画後にペンとブラシの状態を復元する。
  選択枠、回転表示、パスガイド、頂点、ベジェ制御線、範囲選択枠、文字入力枠、フィルター操作表示、
  作成中の直線・パス・円弧・矩形・楕円・角丸矩形、および字間・行間の双方向矢印は
  この共通経路を使用する。黒背景と白背景における中心線／外縁、意味色、ハンドル、および
  描画状態の復元は `ScreenLayoutOverlayPrimitivesTest` で確認する。回転ハンドル内部のマークは
  白い円内で潰れないよう単線とし、文字パスの文字入力中は外接選択枠と回転ハンドルを表示しない。
- グループ化・解除、開閉、内部選択、複数選択、変形、複製、削除、積層順変更、入れ子作成、
  入れ子ナビゲーション、JSON保存・読込まで実装済みである。
- ScreenDesignMakerとフィルターのWin64 Debug／Releaseは、警告0・エラー0でビルド確認済みである。
- 次回の実機確認では、内部グループへ入った直後のUndo／Redo、Escによる段階的な親復帰、既存の
  トップレベルグループを含む再グループ化を重点的に確認する。
- 上記3項目のモデル／コマンド層は `ScreenLayoutGroupNavigationTest` で回帰テスト済みである。
  実機ではショートカット入力、一覧表示、選択枠が同じ状態へ追従することを確認する。
- Object Propertiesは、フィルターUIと色・不透明度UIを同階層の別Frameとして持つ。フィルターUIは
  見出しとアイコン列、クライアント領域の専用一覧、下寄せの詳細設定領域を1つのFrameへまとめ、
  レイアウト変更時に一体で載せ替えられる構造とした。各レイヤーとグループは順序付きフィルタースタックを
  所有し、縁取り、影、ぼかしの追加、削除、有効／無効、左端ハンドルによる並べ替えを一覧から操作できる。
  これらの構造変更と詳細パラメーター編集はUndo／Redo対象とし、フィルタースタックをversion 15 JSONへ
  保存・復元する。縁取り、影、ぼかしはリスト順にSkia描画へ適用し、通常キャンバス、単体サムネイル、
  グループ全体、入れ子グループ、AviUtl2側の共通RGBA出力へ反映する。通常レイヤーとグループの
  複製では各フィルターを独立したインスタンスとして複製する。削除、グループ化、解除、階層移動の
  Undo／Redoはレイヤー本体の所有権を移すため、フィルターの順序、有効状態、全パラメーターを維持する。
  この一連のライフサイクルは `ScreenLayoutFilterLifecycleTest` で回帰テストする。
- フィルター行のスライダーは、縁取り幅、影のぼかし量、ぼかし半径を直接変更する。ドラッグ中は
  連続プレビューし、Undo／Redo履歴にはドラッグ全体を1操作として追加する。縁取りフィルター選択中は
  効果の外周枠と上下左右の白いハンドルを編集キャンバスへ表示し、ハンドルのドラッグで幅を調整する。
  この直接編集は `ScreenLayoutOutlineCanvasEditTest` で回帰テストする。影フィルター選択中は
  元レイヤーから相対オフセットした影枠と中心ガイドを編集キャンバスへ表示し、枠のドラッグで
  Offset X／Yを調整する。ぼかしフィルター選択中は元オブジェクト枠と半径の3倍に相当する点線の
  効果範囲を表示し、上下左右の丸ハンドルで半径を調整する。この直接編集は
  `ScreenLayoutBlurCanvasEditTest` で回帰テストする。フィルター行を選択すると右下の共通カラー
  ピッカーを縁取り色または影色へ自動的に切り替え、影では同じ場所の不透明度も編集する。
  ぼかし選択中は色と不透明度を無効化し、選択解除時はオブジェクト色へ戻す。色ダイアログと
  詳細欄の重複設定は廃止し、ドラッグ全体を1回のUndo／Redoとして扱う。この切り替えは
  `ScreenLayoutFilterColorPickerTest` で回帰テストする。フィルター領域の見出し、追加メニュー、
  フィルター名、色、透明度、補助設定など、利用者へ表示する名称は日本語へ統一する。
- 新規配置の色は `TScreenLayoutPaintStyle` を持つ共通の作成描画スタイルから採用する。種類は単色、
  グラデーション、パターン、テクスチャを定義する。右下ピッカーの直上には4モードのアイコンを
  常時表示し、実装済みの単色と線形グラデーションだけを操作可能にする。パターンとテクスチャは
  実装まで無効表示する。線形グラデーションは単色と別に開始色、終了色、対象ローカル0..1座標の
  方向線を保持し、モードを往復しても各設定を失わない。配置済みグラデーションの選択中も
  モード列は有効とし、単色へ戻す変更をUndo／Redoできる。作成ツール中または
  未選択時のカラーピッカーは「作成色」を編集し、線、塗り図形、通常文字、文字パスの固定初期色を
  廃止する。選択オブジェクトや色付きフィルターの色をユーザーが変更した場合も、その確定色を
  次回作成用へ採用する。選択モードから作成ツールへ切り替える瞬間には、右下のピッカーへ実際に
  表示されている有効な色と透明度を作成スタイルへコピーし、ツール別の以前の色は復元しない。
  配置済みオブジェクトは配置時点の値を独立して保持する。この動作は
  `ScreenLayoutCreationPaintStyleTest`、`ScreenLayoutFilterColorPickerTest`、
  `ScreenLayoutCanvasCreationOverlapTest` で回帰テストする。
- グラデーション直接編集の第1段階として、選択ツールで単一の線形グラデーションオブジェクトを
  選択した場合に、オブジェクト中央を左から右へ結ぶ方向ガイドと色付きの始点・終点を表示する。
  点の当たり判定を線より優先し、点上では移動カーソル、線上では追加を示す十字カーソルへ変更する。
  フィルター選択中と作成ツール中はガイドを表示しない。この表示座標とホバー判定は
  `ScreenLayoutGradientInteractionTest` で回帰テストする。
- 第2段階では線形ガイドの中間点を安定したID、始点からの比率、色、不透明度として保持する。
  線上の左クリックで、その位置の補間色を持つ中間点を追加する。中間点の右クリックは削除し、
  始点と終点の右クリックは操作を消費するが削除しない。点の追加・削除はそれぞれ1件の
  Undo／Redoとし、中間点を含む複数色グラデーションをSkia描画、色プレビュー、JSON保存・読込へ
  反映する。追加・削除と端点保護は `ScreenLayoutGradientInteractionTest` で回帰テストする。
- 第3段階では始点と終点を自由にドラッグして方向と長さを変更し、中間点は直線上へ投影して
  始点からの比率だけを変更する。ガイド線上で押して4ピクセル以上動かした場合は両端を同量移動し、
  動かさずに離した場合は中間点追加として扱う。方向線はオブジェクト外にも配置でき、各ドラッグ全体を
  1件のUndo／Redoとして記録する。この操作は `ScreenLayoutGradientInteractionTest` で回帰テストする。
- 第4段階ではガイド上の始点、終点、中間点をクリックして色編集対象として選択し、右下の共通カラー
  ピッカーへその点の色を表示する。ピッカーのグラデーション表示からも最寄りの点を選択でき、選択位置を
  白線で示す。色変更は選択点だけへ反映し、連続操作全体を1件のUndo／Redoとして記録する。選択中の点は
  キャンバス上でも白い輪郭で区別する。この連携は `ScreenLayoutGradientInteractionTest` と
  `ScreenLayoutFilterColorPickerTest` で回帰テストする。
- 線形グラデーションの最終仕上げでは、レイヤーのローカル範囲と回転を共通の描画基準として扱い、
  回転・拡大表示後も描画方向、編集ガイド、ドラッグ座標を一致させる。通常文字は文字レイアウトの
  拡縮前座標でシェーダーを構築し、図形と同様にオブジェクト範囲へ追従させる。削除やUndo／Redoで
  選択中の中間点が存在しなくなった場合は始点へ戻し、オブジェクト選択解除時は点選択も解除する。
  点ごとの透明度は今回の線形グラデーション完成範囲から外し、将来拡張として保留する。
- 行スライダーの現在値は、白塗り・黒縁の大きめの丸ノブで示す。編集プレビューは表示解像度を上限とし、
  影・ぼかし用のSkia一時レイヤーを効果範囲へ限定する。グループを含まない通常構成では中間の
  全面RGBA合成も省略し、フィルター付きオブジェクト移動時の再描画負荷を抑える。鋭角を持つ太い線の
  フィルター用描画範囲はSkiaのマイター上限まで確保し、縁取りが角で欠けることを防ぐ。このケースは
  `ScreenLayoutFilterRenderTest` で回帰テストする。
- 図形作成ツールの入力は、既存レイヤーの選択枠および開いているグループのヒットテストより優先する。
  既存図形上からドラッグしても新規図形を重ねて作成できる。Line／Path／Shapeの選択済み頂点または
  ベジェハンドルを直接クリックした場合だけは、構造編集を優先する。
- スナップ第1段階として、通常オブジェクト移動と図形作成中の入力点を、キャンバスの辺・中央、
  他の表示オブジェクトの辺・中央へ優先的に吸着し、候補がない場合は表示倍率に応じた不可視グリッドへ
  吸着する。吸着中は該当軸を橙色の点線で示し、他オブジェクトが対象ならその外接枠も強調する。
  Alt押下中は吸着を一時無効化する。Line／Path／Shape／文字パスで1点目の配置後にShiftを押した場合は、
  直前の点から水平または垂直の近い方向へ2点目を拘束する。この動作は
  `ScreenLayoutSnapGeometryTest` で回帰テストする。
- スナップ第2段階として、選択枠のリサイズ辺・角、Path／Shape／文字パスの頂点移動、開いた
  グループ内で選択した子の移動へ吸着を拡張する。頂点移動では選択レイヤー全体を除外せず、移動中の
  頂点だけを除いて同じPath／Shape内の残りの頂点を優先候補にする。新規Path／Shape作成中も、
  先に配置した頂点を後続頂点の優先候補にする。X座標一致は縦ガイド、Y座標一致は横ガイドで表示し、
  Alt押下中はいずれも一時解除する。通常オブジェクトと開いたグループ内の子の回転は、Shift押下中に
  15度刻みへ拘束する。
- 完全に重なったオブジェクトは、選択済みオブジェクトの実形状上からドラッグを開始した場合に、
  前面オブジェクトへ選択を奪われず現在の選択を移動する。移動しない通常クリックは最前面を選択し、
  Alt＋クリックは現在選択より背面にあるカーソル直下のレイヤーへ進み、最背面の次は最前面へ戻る。
  Alt＋ドラッグは選択を維持したままスナップなしで移動する。右クリックメニューの
  「この位置のレイヤー」にはカーソル直下の候補を前面順で表示し、任意のレイヤーを直接選択できる。
  この重なり選択とドラッグ保護は `ScreenLayoutSnapGeometryTest`、候補メニューは
  `ScreenLayoutObjectContextMenuExtensionTest` で回帰テストする。
- フィルター一覧のDocument再同期で選択行が解除されても、現在の作成ツールは変更しない。実際に
  フィルター行を選択した場合だけSelectツールへ切り替える。図形作成開始時の選択解除や作成後の
  新規レイヤー選択によって、Rectangleなどの連続作成モードが解除されないことを回帰テストする。
  線幅は上部ツールバー、線種と線端は同ツールバーの詳細ポップアップ、座標とサイズは編集メニューの
  ポップアップへ分離した。
- テキストレイヤーだけを選択した場合は、上部ツールバーの線幅UIをフォント選択コンボボックスへ
  切り替え、その右側へ太字、斜体、下線、取り消し線のトグルを表示する。複数テキスト選択にも
  一括適用し、変更全体を1回のUndo／Redo対象とする。書体と装飾はversion 15 JSONへ保存する。
- 文字装飾の右側には現在配置を示すアイコンを1つ置き、クリックするとExcelの配置UIに近い3×3の
  配置アイコンを下側へ表示する。選択またはフォーカス喪失で閉じ、複数選択にも一括適用して1回の
  Undo／Redo対象とし、version 15 JSONへ保存する。全体枠フィットでは上下方向に余白がないため、
  上段と下段を無効表示にして中段の左・中央・右だけを操作可能にする。
- 字間と行間は文字サイズに対する追加比率として保持する。数値入力欄は上部ツールバーへ置かず、
  テキスト選択枠の下辺中央と右下の間から少し外側に表示する左右マークで字間を、右辺中央と右下の
  間から少し外側に表示する上下マークで行間をドラッグ調整する。マークは白縁の黒い棒と両端の三角形を
  組み合わせ、明暗どちらの背景でも見分けられるようにする。小さな矢印形状が潰れないよう、隣接する
  中間ハンドルを越えてもよい十分な長さと大きさを確保する。枠操作との誤反応を避けるため、枠線から
  十分に離し、当たり判定は正方形ではなく矢印に沿った細長い範囲とする。
  ドラッグ中だけ現在値を百分率で表示し、Shift併用では変化量を小さくする。字間は折り返し、描画、
  選択範囲、キャレット位置へ、行間は行送りとキャレット移動へ共通して反映し、複数選択、複製、
  Undo／Redo、version 15 JSON保存・復元へ対応する。過大な間隔による極端な折り返しと選択枠変形を
  防ぐため、字間比率は-50%から+100%、行間比率は-50%から+300%へ制限する。
- レイヤー一覧は名称、サイズ、透明度などの文字情報を表示せず、サムネイル、表示、ロック、
  グループ開閉だけを持つ。初期幅は150pxとし、外側スプリッターの増減分を一覧へ反映して幅を保存する。
- グループ編集中のレイヤー一覧は別ページへ切り替えず、グループ行を元の位置に残したまま、その下側へ
  子を表示する。行自体は階層ごとにインデントせず、左側の括弧状の範囲線でグループの展開範囲を示す。
- レイヤー一覧のD&Dは、同一階層の積層順変更に加え、グループ行の中央へのドロップでそのグループへ
  移動し、展開されたツリーの任意の行間へドロップして親コンテナと積層位置を同時に指定する。移動後は
  階層を開いて対象を選択し、Undo／Redoでは所有権、階層、選択をまとめて復元する。
- グループ関連のUndoコマンドは、操作窓口の `ScreenLayoutGroupCommands`、子の所有権と構造変更を
  扱う `ScreenLayoutGroupChildCommands`、変形を扱う `ScreenLayoutGroupTransformCommands` に分離した。
  キャンバス上の子選択・当たり判定・ドラッグ状態は `ScreenLayoutGroupInteraction` が担当する。
