# ScreenDesignMaker デザイン機能要求整理

作成日: 2026-09-03

## 1. 目的

ScreenDesignMaker で、サムネイル・ロゴ・画面レイアウト等のデザインを効率よく作成するために必要となる機能を整理する。

本資料では、書籍等で紹介される「効果的なサムネイル」の構図・文字・図形・画像加工を、ScreenDesignMaker の既存設計思想へ落とし込んだ要求事項をまとめる。

基本方針は以下とする。

- 専用機能を増やしすぎず、既存の図形・線・文字・パス・グラデーション・マスク等を組み合わせて実現する。
- オブジェクト種別ごとに個別設定画面を作らず、共通プロパティパネルを再利用する。
- キャンバス上で直接操作できるものは、数値入力より直接編集を優先する。
- 元データを可能な限り破壊しない。
- 参照型画像については、加工が必要な場合のみ内部キャッシュやテンポラリ実体を生成する。

---

# 2. 構図・視線誘導

## 2.1 スポットライト

見せたい場所へ視線を誘導するための機能。

要求候補:

- スポットライト効果
- 背景の減光
- 周辺暗化
- 円形・楕円形ハイライト
- 任意形状マスクによる強調
- 境界ぼかし
- 光彩・グロー
- 強調部分の明るさ補正
- 背景の彩度低下
- 背景ぼかし

既存のマスク・ぼかし・透明度等を利用して実現可能とする。

---

## 2.2 日の丸構図

中央に円や主役を配置し、その周囲へ文字やオブジェクトを配置する。

要求候補:

- 中央円ガイド
- 円形レイアウト配置
- 円周上への均等配置
- 円周に沿った回転配置
- 放射状ガイド
- 中央基準整列
- 同心円ガイド
- 円配置の開始角
- 時計回り・反時計回り

---

## 2.3 N分割構図

二分割・三分割だけでなく、N分割を共通機能として扱う。

要求:

- N分割ガイド
- 数字キー 1～9 による分割数指定
- 縦分割
- 横分割
- 縦横同時分割
- 分割線へのスナップ
- 分割交点へのスナップ
- 分割領域を基準とした配置
- 分割比率変更

### 表示方法

キャンバス内部へ常時グリッドを描画するとデザインが見えづらくなるため、通常時はキャンバス外側へトンボ線のような分割位置マーカーを表示する。

- 上下左右の外側へ短い分割マーカーを表示
- 内部には仮想スナップ線だけを保持
- スナップ時のみ対象線を一時表示
- 必要時のみ内部ガイドを表示

---

## 2.4 阿吽型構図

機能として専用実装する必要性は低いが、構図ガイド・テンプレート候補として扱う。

---

## 2.5 額縁型構図

要求候補:

- 中空四角形
- 中空円形
- ドーナツ図形
- 内側くり抜き
- ブーリアン減算
- 額縁幅調整
- 内外余白調整

既存の図形加減算を利用する。

---

# 3. 配置・整列・スナップ

## 3.1 オブジェクト整列

選択オブジェクトまたはグループに対して使用する。

要求:

- 左揃え
- 水平中央揃え
- 右揃え
- 上揃え
- 垂直中央揃え
- 下揃え
- 水平方向均等配置
- 垂直方向均等配置
- オブジェクト間隔均等化
- キャンバス基準整列
- 選択範囲基準整列
- 基準オブジェクト固定整列
- グループ内部整列

---

## 3.2 角度スナップ

位置だけでなく角度もスナップ対象とする。

要求:

- 既定角度へのスナップ
- 他オブジェクトの角度へのスナップ
- 平行角度
- 直交角度
- ガイド線角度
- スナップ候補角度の表示
- 一時スナップ切替
- 複数オブジェクト間の角度統一

---

# 4. 図形

## 4.1 図形加減算

図形の組み合わせで、専用機能を増やさず多くの形状を作れるようにする。

要求:

- 加算
- 減算
- 交差
- 結合
- 分離
- 閉じたパス同士の演算

利用例:

- 吹き出し
- 額縁
- ドーナツ
- ロゴ
- 切り抜き領域
- マスク領域

吹き出し専用オブジェクトは原則不要とする。

---

## 4.2 図形の役割

通常図形に役割フラグを持たせる。

役割:

- 通常
- マスク
- クリッピング

図形自体は通常図形と同じ編集機能を使用する。

- 移動
- 拡大縮小
- 回転
- 頂点編集
- ベジェ編集
- Undo / Redo

レイヤー表示では役割がわかるようにする。

- M = Mask
- C = Clipping

適用対象はレイヤー位置で決める。

基本候補:

- 直下1レイヤー
- 直下1グループ

複数対象が必要な場合はグループ化で対応する。

---

# 5. マスクとクリッピング

## 5.1 クリッピング

図形の形状だけを利用する。

- 色は無関係
- 不透明度も基本的に無関係
- 内側表示 / 外側非表示
- 反転可能

内部的には二値マスクとして扱える。

---

## 5.2 マスク

図形の形状に加えて最終アルファ値を利用する。

- アルファ100% = 完全表示
- アルファ50% = 半透明
- アルファ0% = 非表示

これにより既存機能を流用可能。

- グラデーションマスク
- パターンマスク
- ぼかし境界
- 透明度マスク

色そのものはマスク判定に使用せず、最終アルファのみ参照する方針とする。

---

## 5.3 画像への適用

写真・画像もマスクおよびクリッピング対象にできる。

- 円形クリッピング
- 斜めクリッピング
- 任意パスクリッピング
- グラデーションマスク
- パターンマスク

画像側に専用の加工機能を持たせず、通常レイヤー対象として扱う。

---

# 6. 画像切り抜き

マスク・クリッピングとは別に、確定型の「切り抜き」機能を持つ価値がある。

## 6.1 切り抜き方法

閉じたパス図形で領域を作る。

- パス加算
- パス減算
- 交差
- 反転

最終領域を画像アルファへ適用する。

---

## 6.2 参照型画像への対応

元画像は参照型のため、元ファイルを書き換えない。

切り抜き確定時:

- メモリ上に加工後ビットマップを生成
- またはテンポラリ画像を生成
- 元画像参照は保持

確定後の加工結果は変更不能としてもよい。

確定後に許可する操作:

- 移動
- 拡大縮小
- 回転

再切り抜きが必要な場合は元画像から再生成する。

保持候補:

- 元画像パス
- 切り抜きパス情報
- 加減算情報
- 加工キャッシュ

---

# 7. 塗り

## 7.1 基本概念

「塗り」はオブジェクト種別ではなく、塗る領域を持つオブジェクトが利用する共通設定とする。

対象例:

- 図形
- 閉じたパス
- 文字

対象外例:

- 通常画像
- 単純線

---

## 7.2 塗り方式

大分類:

- なし
- ベタ塗り
- グラデーション
- パターン

塗り方式ごとに専用設定を表示する。

---

## 7.3 ベタ塗り

設定例:

- 色
- 不透明度

---

## 7.4 グラデーション

グラデーションはフィルターではなく、塗り方法そのものとする。

UIには最低限以下を表示する。

- グラデーション
- 種別

種別を切り替えたとき、キャンバス上へ直接編集用ガイドを表示する。

例:

- 線形 → 始点・終点を示す線
- 放射 → 中心点と円
- 円形 → 中心と半径
- その他の形状 → 専用ガイド

設定候補:

- 種別
- 始点
- 終点
- 角度
- 中心
- 半径
- 色停止点
- 停止点位置
- 停止点色
- 停止点透明度
- グラデーション解除

グラデーションの始点・終点は、線オブジェクトの始点・終点とは別物であり、描画材自身の座標系とする。

---

## 7.5 パターン

パターンは塗り方式の一種とする。

要求候補:

- 水玉
- 縞模様
- 斜線
- チェック
- ドット
- ユーザー定義パターン

設定:

- パターン選択
- ファイル名
- サムネイル
- サイズ
- 回転
- 間隔
- 色
- 背景色
- 密度
- 位置ずらし
- 繰り返し

---

## 7.6 将来候補

同系統の描画材として将来的に検討可能。

- テクスチャ
- 画像塗り
- ハッチング
- ノイズ
- スクリーントーン
- メッシュグラデーション

---

# 8. 線・連続線・自由線

## 8.1 線

固有設定:

- 始点
- 終点
- 太さ
- 線種
- 始端形状
- 終端形状
- 始端矢印
- 終端矢印
- 矢印サイズ

---

## 8.2 連続線・曲線

固有設定:

- 頂点
- ベジェ制御点
- 開く / 閉じる
- 線種
- 太さ
- 結合形状
- 矢印
- 塗り（閉じた場合）

---

## 8.3 自由線

固有設定:

- 基本太さ
- 筆圧
- 筆圧カーブ
- 筆先形状
- 平滑化

---

# 9. 飾り罫

専用の飾り罫オブジェクトを増やさず、線にグラデーション等を適用して実現する。

候補:

- 線方向グラデーション
- 線幅方向グラデーション
- 両端フェード
- 中央強調
- 複数色
- 繰り返し

---

# 10. 縁取り

既存縁取りに加えてオフセットを持たせる。

要求:

- Xオフセット
- Yオフセット
- 多重縁取りごとの個別オフセット
- 回転追従
- 本体との相対位置保持

用途:

- ポップ表現
- 立体感
- 手作り感
- 影のような縁取り

---

# 11. 簡易3D

凝った3D機能ではなく、サムネイル向け疑似立体を目的とする。

対象:

- 文字
- 図形

要求候補:

- 押し出し
- 奥行き
- 押し出し方向
- 側面色
- 正面色
- 明度調整
- ベベル風表現
- 複製ずらし
- 連続複製
- 立体部分の分解

影:

- 自動生成
- 手動分離
- 落ち影
- 接地影
- ぼかし
- 硬い影
- 色
- 透明度
- 光源方向

---

# 12. オブジェクト不透明度・ぼかし

既存検討済み機能として扱う。

- オブジェクト不透明度
- ぼかし
- 両者の併用

---

# 13. 手書き文字・ロゴ化

専用ロゴ機能を増やさず、既存の自由線・文字図形化・パス編集で対応する。

要求:

- 自由線による文字
- フォント文字の図形化
- 文字パス化
- 頂点編集
- ベジェ編集
- 一部削除・追加
- 文字結合
- 塗り
- 縁取り
- グラデーション
- パターン

---

# 14. テキストオブジェクト

## 14.1 基本テキスト

通常テキストは指定領域へ整然と配置する。

設定候補:

- 文字列
- フォント
- サイズ
- 太字
- 斜体
- 字間
- 行間
- 横書き
- 縦書き
- 左揃え
- 中央
- 右揃え
- 上下揃え
- 折り返し

---

## 14.2 パス追従テキスト

文字専用の独自パスGUIを作らず、既存の連続線オブジェクトを内包するテキストとして扱う。

内部構造:

- テキスト本体
- 配置用連続線

レイヤー上では1オブジェクトとして扱う案を優先する。

内部の連続線は既存の連続線と同じデータ構造・UIを再利用する。

操作:

- テキスト入力
- 初期状態で文字下に直線パス
- パス移動で文字追従
- パス変形で再配置
- 頂点編集
- ベジェ編集
- 直線から曲線へ変更

---

## 14.3 パス追従の接触辺

横書き:

- 上面
- 下面

縦書き:

- 左面
- 右面

その他:

- パスオフセット
- 進行方向反転
- 文字向き反転
- パス追従ON/OFF

---

## 14.4 曲線文字の衝突回避

谷方向へ文字を沿わせた場合、文字上部が重なる可能性がある。

まずは文字を回転矩形として扱う。

処理:

- 各文字の外接矩形を取得
- パス接線方向へ回転
- 前文字矩形との交差判定
- 交差時はパス上を先へ送る
- 接触しなくなった位置で確定

設定候補:

- 自動衝突回避 ON/OFF
- 最小余白
- 自動字間補正

グリフ輪郭同士の精密判定は将来拡張とする。

---

# 15. 縦書き

日本製アプリとして縦書き対応を考慮する。

単純に1文字ずつ縦へ並べるだけでは不十分。

注意事項:

- 句読点
- 括弧
- 長音符
- 波ダッシュ
- 英数字
- 縦中横
- 禁則処理
- 行送り
- フォントの縦組み字形
- 縦書きメトリクス

Windowsの @ 付きフォントは縦書き用派生フォントとして利用されるが、それだけで完全な縦組みになるわけではない。

サムネイル用途では長文組版よりも、短い見出し・タイトルを自然に表示できることを優先する。

---

# 16. オブジェクト情報パネル

## 16.1 方針

オブジェクト種別ごとに設定画面を作らない。

小さな設定パネル部品を用意し、選択オブジェクトに必要なものだけ縦方向へ並べる。

例:

- 座標
- サイズ
- 回転
- 塗り
- 線
- 縁取り
- 影
- ぼかし
- 文字
- 矢印
- マスク / クリッピング
- グラデーション
- パターン

---

## 16.2 Capability方式

オブジェクト種別ではなく、どの機能を持つかでUI表示を決定する。

例:

- 塗りを持つ
- 線を持つ
- 影を持つ
- 文字情報を持つ
- パス編集可能
- マスク化可能
- クリッピング化可能

新しいオブジェクトを追加した場合でも、既存パネルを再利用できるようにする。

---

## 16.3 UI構造

右側に縦スクロール可能なプロパティ領域を持つ。

- 必要パネルだけ表示
- 縦積み
- 表示順固定
- 折りたたみ
- 再利用
- 選択変更時に再配置
- 毎回破棄せず保持して付け替える方式も候補

---

# 17. オブジェクト種別と主な情報

| 項目 | 図形 | 線 | 連続線 | 自由線 | 文字 | 画像 | グループ |
|---|---|---|---|---|---|---|---|
| X / Y | ○ | ○ | ○ | ○ | ○ | ○ | ○ |
| 幅 / 高さ | ○ | △ | ○ | ○ | ○ | ○ | ○ |
| 回転 | ○ | ○ | ○ | ○ | ○ | ○ | ○ |
| 不透明度 | ○ | ○ | ○ | ○ | ○ | ○ | ○ |
| 塗り | ○ | - | △ | - | ○ | - | - |
| グラデーション | ○ | - | △ | - | ○ | - | - |
| パターン | ○ | - | △ | - | ○ | - | - |
| 線 | ○ | ○ | ○ | ○ | △ | - | - |
| 線幅 | ○ | ○ | ○ | ○ | △ | - | - |
| 線種 | ○ | ○ | ○ | ○ | △ | - | - |
| 線端 | - | ○ | ○ | ○ | - | - | - |
| 矢印 | - | ○ | ○ | △ | - | - | - |
| 縁取り | ○ | △ | △ | △ | ○ | ○ | - |
| 影 | ○ | ○ | ○ | ○ | ○ | ○ | △ |
| ぼかし | ○ | ○ | ○ | ○ | ○ | ○ | △ |
| 頂点編集 | ○ | - | ○ | ○ | △ | - | - |
| ベジェ編集 | ○ | - | ○ | ○ | △ | - | - |
| マスク化 | ○ | - | ○※閉 | △ | ○※図形化 | - | - |
| クリッピング化 | ○ | - | ○※閉 | △ | ○※図形化 | - | - |
| 3D化 | ○ | - | △ | △ | ○ | - | - |

---

# 18. オブジェクト固有情報

## 図形

- 図形種類
- 角丸
- 頂点数
- 中空
- 内側サイズ
- ShapeRole
  - 通常
  - マスク
  - クリッピング

## 線

- 太さ
- 線種
- 始端形状
- 終端形状
- 始端矢印
- 終端矢印
- 矢印サイズ

## 連続線

- 頂点
- ベジェ制御点
- 開閉
- 結合形状
- 矢印

## 自由線

- 太さ
- 筆圧
- 筆圧カーブ
- 筆先形状
- 平滑化

## 文字

- 文字列
- フォント
- サイズ
- 太字
- 斜体
- 字間
- 行間
- 横書き
- 縦書き
- 整列
- 図形化
- 内包パス
- パス追従

## 画像

- 参照ファイル
- 元サイズ
- アスペクト比
- 切り抜き状態
- 加工キャッシュ

## グループ

- グループ名
- 全体不透明度
- 全体変形
- マスク / クリッピング対象としての扱い

---

# 19. 抽象表現

抽象芸術・アブストラクト専用エンジンは原則作らない。

既存機能の組み合わせで対応する。

- 自由線
- 図形
- グラデーション
- パターン
- 透明度
- ぼかし
- 重なり
- ランダム配置補助

必要ならテンプレート・プリセットとして提供する。

---

# 20. 追加で有用な補助機能

サムネイル制作全般で有用。

- セーフエリア
- 余白ガイド
- 背景と前景のコントラスト確認
- 色数制限補助
- 配色補助
- 矢印
- 集中線
- 重なり順による奥行き
- 複数オブジェクト一括変形
- 構図テンプレート
- 縮小プレビュー
- 小サイズ時の可読性確認
- 完成画像比較

---

# 21. 全体設計方針

今回の検討で特に重要となった方針。

1. 専用オブジェクトを増やしすぎない。
2. 図形加減算・連続線・パス・塗り等の汎用機能を再利用する。
3. グラデーション・パターンは「塗り方式」として扱う。
4. マスク・クリッピングは通常図形へ役割を付与する。
5. 画像切り抜きはマスクとは別の確定加工として扱う。
6. オブジェクト情報はCapability方式で共通パネルを組み立てる。
7. キャンバス上で直接編集できる要素は直接操作を優先する。
8. パス追従文字は既存の連続線を内包し、連続線UIを再利用する。
9. 日本語向けとして縦書きを考慮する。
10. サムネイル制作に必要な表現力を優先し、組版・3D・画像編集は過剰に複雑化させない。

